# Progress Log

## 2026-08-15 管理端套餐可见性与刷新时间

- 用户已明确要求提交到 main 并上线；开始执行准确提交构建、生产部署和健康复查。
- 最终审查问题已全部修复：请求准入和套餐摘要都只读取当前额度窗口，没有记录统一按已用 0；已删除会随用户数量膨胀的历史用量查询。
- 管理员用户列表和用户套餐中心已拆分“今日剩余”与受周/月等限制后的“当前剩余”；仍生效但已下架的套餐使用服务端返回的真实套餐名称。
- 新增回归测试确认历史用量记录不会在当前窗口缺失时参与计算；SQLite 和隔离 PostgreSQL 实库测试、额度单元测试、管理端与用户接口测试、套餐中心 5 项测试、前端类型检查和生产构建全部通过。
- Rust 全工作区编译与严格 Clippy、目标前端 ESLint 和 Git 差异检查通过；ESLint 只保留用户列表中两个未修改旧输入框的格式警告。
- 本轮未提交、未推送、未部署，也没有修改生产数据。
- 用户确认当前额度记录不存在时一律按已用 0 处理；开始删除套餐摘要中的历史明细查询，并修复每日额度混合显示和下架套餐名称问题。
- 用户同意执行最终审查修复；已先更新架构记录，明确页面与扣费共用当前数据库窗口、正常读取不扫描全部历史明细、摘要失败不拖垮页面、套餐操作后立即刷新列表。
- 已从远端最新 `main` 创建独立工作树 `/private/tmp/aether-admin-user-plan-summary-20260815`，分支 `codex/admin-user-plan-summary`。
- 已核对生产用户额度窗口和请求明细，确认滚动刷新时间及快速耗尽原因。
- 已确认用户列表接口和页面都没有套餐摘要字段。
- 已完成 UI 路由：Web 管理后台、现有页面轻量扩展、紧凑表格信息展示。
- 已完成架构记录：刷新时间由服务端按套餐生效时间计算滚动 24 小时窗口，前端不自行推算。
- 已实现 PostgreSQL 单次批量查询，以及 MySQL、SQLite 固定次数批量查询；管理端用户列表不会按用户逐条访问数据库。
- 管理端用户列表已增加独立“套餐”列和移动端套餐信息，显示套餐名称、实际剩余额度、下次刷新时间和到期时间。
- 用户套餐中心已显示今日剩余、今日已用和下次刷新时间；其他周期额度更严格时，另外显示实际可用的“当前剩余”，不再混入今日剩余。
- 已覆盖无套餐、每日额度、月额度耗尽和无每日额度套餐的显示语义。
- 后端额度计算、SQLite 查询、真实 PostgreSQL 迁移与套餐摘要查询、管理端接口和用户套餐接口测试通过；PostgreSQL 临时测试库已删除。
- 前端套餐中心 3 项测试、类型检查、目标代码规范检查和生产构建通过。
- Rust 全目标 Clippy 严格检查、Git 差异检查和 UI 静态/运行时审查通过；未提交、未推送、未部署。
- 审查修复已完成：每日、5 小时、每周和每月额度统一优先使用尚未结束的数据库窗口，管理员修改套餐时间不会使页面与请求扣费使用不同窗口。
- 套餐摘要和请求准入都不再读取历史扣费明细；缺少当前窗口记录时统一按已用 0 处理。
- 套餐摘要失败不再使管理员用户列表或用户套餐中心返回错误；接口保留套餐基本信息并返回明确状态，前端显示“暂时无法读取”，服务端记录错误日志。
- 管理员发放、修改或取消套餐成功后，用户列表立即重新读取，套餐列不再保留旧内容。
- SQLite 测试首次发现旧额度查询的 SQL 条件拼接错误；已同步修复三种数据库实现并复测通过。
- Phase 6 验证通过：Rust 编译与严格代码检查；26 项额度及相关结算测试；SQLite 与真实 PostgreSQL 套餐窗口测试；管理员列表和用户套餐接口失败隔离测试；套餐中心 4 项前端测试；前端类型检查和生产构建；Git 差异检查。PostgreSQL 临时测试库已删除。
- 本轮未提交、未推送、未部署，也没有修改生产数据。

---

## 2026-08-13 老套餐实时跟随套餐号池

- 用户已完成生产中四个套餐的号池设置。
- 已确认当前版本只保存套餐模板，不会自动改变老用户；新逻辑上线前老用户仍按购买或发放时保存的旧范围使用。
- 已从远端 `main` 的生产提交 `2348a4560` 创建独立工作树和分支 `codex/live-plan-provider-scope`，没有改动原工作区。
- 已启用文件化计划和现有后台 UI 规范；当前先更新架构文档，再修改业务代码。
- 已完成实现：所有有效老套餐从下一次请求起直接读取套餐当前号池；模型按当前有效号池、有效模型关系和有效全局模型自动更新，不再读取购买时的旧模型清单。
- 已删除用户级套餐号池修改入口；旧客户端继续提交该字段时返回明确错误。后台用户套餐列表、用户套餐中心和模型目录均显示当前套餐配置，空号池明确表示套餐不能用于新请求。
- 已保留请求开始时的计费记录：套餐号池变更不影响已经开始的请求；上线切换期间由旧版本生成的空号池记录仍可正常结算，不会变成免费请求。
- 请求开始时仍使用原有一次数据库查询，仅替换为读取套餐当前号池及当前模型关系；没有增加数据库往返。后台套餐列表原先逐条读取用户号池，现已合并为一次查询。
- PostgreSQL、MySQL、SQLite 和内存实现已经统一；PostgreSQL 新增用例在隔离本地数据库真实执行通过，SQLite 与内存动态变更用例通过。
- 后端严格代码检查通过；数据层 532 项中 531 项通过，唯一失败是远端 `main` 已存在的迁移测试数据缺失数据库必填字段，与本次套餐修改无关，单独复测仍稳定失败。
- 前端类型检查、相关套餐测试和生产构建通过；目标代码规范检查没有错误，保留 2 条未修改旧输入框的格式警告。套餐管理与用户套餐显示通过 `PASS ui-review gate`。
- 用户已明确授权提交、合并到 main 并部署生产；正在按 test 验证、main 晋级和准确提交发布流程执行。

---

## 2026-08-15：rn-hybrid PostgreSQL 无感切换实施

- 已进入正式复审阶段；使用文件化计划记录硬门槛、生产变更和验证结果。
- 当前尚未修改任何服务器；先核对真实部署和备份条件，文档更新完成后再开始生产准备。
- 已完成第一轮配置复审：旧主库具备在线建立物理复制的条件；两台 Frontdoor 可以使用旁路容器和 Caddy 热重载迁移到稳定代理。
- 已修正代理设计：PgBouncer 使用事务池和命名预处理语句跟踪，不使用会话池。代码中现有数据库特性与事务池兼容，DDL/迁移明确绕过代理。
- 发现 hd0526 与 OVH 运行不同应用镜像；本次分别沿用当前镜像，避免数据库迁移与应用升级混在一起。
- 已新增正式迁移记录 `docs/operations/rn-hybrid-postgres-switchover.md`，写明目标、非目标、网络、代理、复制、切换、验收、影响和提升前后不同的回退边界。
- 方案复审通过：没有需要停止执行的架构问题。现在进入 rn-hybrid 维护与私网准备阶段。
- 已新增并通过 Bash 语法检查的 `scripts/rn-hybrid-postgres-prepare.sh`；脚本会先备份配置，再更新系统、安装数据库迁移依赖、加固 SSH 入站边界并保留现有 80/443。
- 已记录 rn-hybrid 重启前业务基准；接下来执行准备脚本和维护重启。
- rn-hybrid 准备脚本已成功完成：系统更新、Docker 29.1.3、Compose 2.40.3、PgBouncer 1.22.0、WireGuard、UFW、Fail2ban 和自动安全更新均已就绪；PgBouncer 保持禁用，尚未承载流量。
- 重启前复核通过：SSH、Nginx、session2sub2api、PostgreSQL 16、Docker、Fail2ban 和自动更新均 active；本机与公网 HTTPS 均返回 200，PostgreSQL 16.14 可正常查询；根分区可用 444 GB。
- UFW 已采用默认拒绝入站，只公开 22/80/443，并且 UDP 51821 仅允许 rn01、hd0526、OVH 三个固定地址访问。下一步重启载入 6.8.0-137 内核并执行两轮恢复检查。
- rn-hybrid 已重启进入 6.8.0-137 内核，首次复核和延迟 10 秒后的第二次复核均通过：全部既有服务 active、factory2 本机和公网请求成功、PostgreSQL 16 数据库约 672 MiB、0 个失败单元、0 条本次启动 error 日志，且不再要求重启。
- rn-hybrid 维护阶段完成，没有影响 Niffler 主站。现在进入四节点 `wg-db` 私网配置。
- 四节点独立 `wg-db` 已部署并设为开机启动：rn-hybrid `10.72.0.1`、rn01 `10.72.0.2`、hd0526 `10.72.0.3`、OVH `10.72.0.4`。原有 `wg0` 未修改，四台配置均已保存在各自 `/root/niffler-wg-db-20260814T173312Z`。
- 四端双向 ICMP 和有效握手均通过；rn01 新增只绑定 `10.72.0.2:5432` 的 systemd TCP 转发，rn-hybrid 通过该入口执行 `pg_isready` 成功。rn01 现有 PostgreSQL 15.18 仍是主库，查询显示 `pg_is_in_recovery=false`。
- 私网变更后独立复核通过：Niffler 返回 200，factory2 返回 200；rn-hybrid 原有 Nginx、PostgreSQL 16、session2sub2api、Docker 全部 active。Phase 3 完成，进入独立 PostgreSQL 15.18 与 PgBouncer 部署。
- 副本构建首次执行被版本字符串前置检查拦截：服务端返回 `15.18 (Debian …)`，脚本原本要求精确等于 `15.18`。已改为核对 `server_version_num=150018`；复核确认尚未创建复制账号、HBA 规则、新数据或 Compose，生产未被修改。
- 第二次执行完成复制账号、8 GiB WAL 上限、新实例配置和镜像准备后，`pg_basebackup` 被 HBA 拒绝，未传输数据、未创建复制槽。根因是 rn01 TCP 转发让后端看到 rn01 本机地址；已修正规则并记录外层 WireGuard/iptables 与内层专用角色/TLS/SCRAM 的双重边界。旧库 healthy、剩余 21 GiB、Niffler 返回 200。
- 第三次执行仍读到旧 HBA。对比证实宿主机文件已正确，容器挂载源却是被原子替换后保留的 `//deleted` 旧 inode；PostgreSQL 已解析规则中没有复制账号。已将脚本改为保留 inode 写入，并加入无需重启生产库的单文件挂载同步与恢复只读步骤；再次确认复制槽为 0、新数据目录为空。
- 已在不重启 rn01 PostgreSQL 的情况下同步运行中 HBA 挂载并恢复只读；复制协议 `IDENTIFY_SYSTEM` 实测成功，返回旧主库系统标识 `7644483892823924774`。
- 57,845,590 KiB 物理基础备份已完成，新 `niffler-postgres15` 以 PostgreSQL 15.18 只读副本启动，只绑定 rn-hybrid `127.0.0.1:55432`；新旧 system identifier 一致，复制槽 `niffler_rn_hybrid` active/reserved。
- 两轮复制复核均为 streaming 且 WAL 差 0 字节；新库无 FATAL/PANIC/ERROR，rn-hybrid 无失败 systemd 单元。用户、API Key、Provider、Provider Key、模型、订单、系统配置和迁移记录八组计数与旧主库完全一致。
- PgBouncer 1.22 已启用并只监听 `10.72.0.1:6432`，后端仍为 rn01。事务池、客户端/后端 TLS、300 客户端上限、60 后端上限、256 个命名预处理语句和 120 秒查询排队均已从运行配置复核。
- PgBouncer 使用真实 `niffler_app` 完成临时表读写、事务级 advisory lock、20 次 prepared 模式事务，并实测 `PAUSE aether` 后一个客户端进入等待、`RESUME` 后返回成功。hd0526/OVH 私网端口可达，公网 6432 不可达，代理无 error 日志。
- 已完成 rn-hybrid R2 备份、定时器和隔离恢复验证脚本；下一步执行人工新备份、重新下载、SHA-256 检查和无端口 PostgreSQL 15.18 全量恢复。成功前 rn01 原定时器保持不变。
- rn-hybrid 已生成并上传新备份 `20260814T192002Z`，大小 34,917,062,653 字节；R2 远端大小与 SHA-256 校验均通过，状态为 success，Telegram 成功通知已送达。编排 SSH 在已结束的 systemd start 上滞留，确认无远端任务后已终止；未重复备份，继续单独执行恢复验证。
- 首次隔离恢复完成重新下载和两遍 SHA-256 校验，并进入 `pg_restore`，但在导入阶段退出且旧清理逻辑删除了诊断现场。系统无 OOM、磁盘、Docker 或复制错误；rn01 定时器仍未交接。恢复脚本已改为持久阶段/verbose 日志，失败时保留下载和数据目录，下一轮以日志为准。
- 第二次隔离恢复成功：R2 对象重新下载后的两遍 SHA-256 校验一致，PostgreSQL 15.18 无端口临时实例完成全量 `pg_restore`；127 张公开表、62 个迁移、317 个用户、303 个 API Key、29 个 Provider 和用量数据可读，验证状态已持久化，临时容器、下载和数据目录自动清理。
- rn-hybrid 备份定时器已启用并等待下一次 04:30（Asia/Shanghai）运行；rn01 旧定时器已停用，旧脚本和历史文件保留。新备份状态、恢复状态、WAL 零延迟、PgBouncer、磁盘和网站 200 均复核通过，Phase 4、5 完成。
- 进入 Phase 6：两台 Frontdoor 先通过旁路容器迁到稳定 PgBouncer 地址，再单独迁移 hd0526 Background；尚未停止旧主库或提升副本。
- hd0526 旁路 Frontdoor 首次暴露 PgBouncer 默认拒绝 SQLx 的 `extra_float_digits`；加入随包官方文档允许的单项忽略配置并 reload 后，旁路四项直连检查、两轮公网检查、PgBouncer 连接和副本零延迟均通过。Caddy 已热重载，原 Frontdoor 保留且没有存量上游连接。
- OVH 旧镜像新启动时因缺少数据库已应用的 `20260809130000` 拒绝运行；已复制并按镜像 ID、revision 和迁移清单校验 hd0526 当前精确镜像。OVH 旁路通过 `aether_ovh` 独立别名完成会话池启动和事务池重连，Caddy 热重载后 `us1`、源站与四项数据库接口连续两轮为 200，旧容器保留且无存量上游连接。
- SQLx 启动校验会使用会话级迁移锁；hd0526 首轮在事务池遗留的空闲后端锁及 OVH 已退出等待连接已按精确 PID 终止，前台持续 200。`aether_ovh`、`aether_background` 只在未接流量或旧 Background 已停止时使用一次会话池启动，随后分别 KILL/RESUME 并确认事务池重连、无 advisory lock。
- hd0526 新 Background 已 healthy，旧 Background 保持停止。重连瞬间出现两次旧 prepared statement 缓存错误，维护任务自动重试；随后 70 秒及第二轮检查没有新增数据库错误，前台未受影响。Phase 6 完成，进入最终主库提升。
- 最终切换时先停止 Background，再暂停 `aether`、`aether_ovh`、`aether_background` 三个别名；旧主库活动事务为 0。rn01 PostgreSQL 干净停止，控制文件状态为 `shut down`，最终 checkpoint 为 `118/CF8B8A0`、时间线 1。
- rn-hybrid 副本接收和重放均到 `118/CF8B918`，超过旧主库最终 checkpoint 120 字节后才提升。新主库仍为 PostgreSQL 15.18，`pg_is_in_recovery=false`，checkpoint 与 WAL 文件均确认时间线 2。
- PgBouncer 三个别名已改为 `127.0.0.1:55432`，RECONNECT/WAIT_CLOSE 完成后恢复；新主库直连写和三个别名的临时表写事务都成功，等待队列为 0。Background 随后恢复 healthy。
- 两轮切换后复核中，用户、API Key、Provider 和迁移计数分别为 317、303、29、62，用量数据持续写入；两台 Frontdoor、Background、新 PostgreSQL、PgBouncer、备份定时器均健康，所有入口和公开数据库接口均为 200，切换后应用 5xx 为 0，没有新增数据库或代理错误。
- rn01 旧 PostgreSQL 已设置 `restart=no`，两个旧私网转发、旧备份定时器均 disabled/inactive；hd0526 与 OVH 旧 Frontdoor、旧 Background 也已在确认无存量连接后停止并设置 `restart=no`。所有旧容器、数据、脚本和配置保留，没有删除。
- Phase 7、8 完成。当前新 Frontdoor/Background 旁路容器使用 `restart=unless-stopped`；在下次应用发布或主动重启前，必须先按运维文档处理事务池启动迁移锁，并将旁路配置正式纳入 Compose，禁止直接运行旧 Compose 服务。
- 已完成迁移收尾文档和正式部署配置：新增 hd0526 Compose 与 rn-hybrid 最终 PgBouncer 配置，OVH Compose 补齐 30 秒数据库连接等待和 TLS 要求；两台服务器环境文件已改为三个独立 PgBouncer 入口，权限保持 `0600`。
- PgBouncer 三个应用入口已固定为会话连接。切换时旧事务池连接短暂出现预处理语句缓存警告，原因是旧连接跨连接方式继续复用；没有公开接口 5xx。正式 Frontdoor 使用新连接启动后错误停止，数据库迁移锁复核为 0。
- hd0526、OVH 的正式 Frontdoor 已分别通过本机健康和数据库接口验证，Caddy 热更新到稳定名称；旧 Frontdoor 连接归零后停止并设置 `restart=no`。
- 正式 Background 已在临时 Background 停止后启动并保持 healthy，旧 Background 设置 `restart=no`；整个过程始终只有一个 Background。
- 两台服务器完整执行 `docker compose up -d` 成功，Caddy 未重建。两轮公开入口和数据库接口全部为 200，新服务数据库错误和应用 5xx 为 0，PgBouncer 等待队列为 0，PostgreSQL 无残留 advisory lock，根分区可用约 387 GiB。Phase 9、10、11 完成，应用可恢复正常提交和固定版本发布流程。

---

## 2026-08-15：Niffler 前端冷启动优化与 rn-hybrid 数据库评估

- 已开始两条只读取证：前端冷启动资源与初始化链；rn-hybrid 硬件、磁盘、网络与现有负载。
- 本轮不会迁移数据库或修改生产配置；先形成基于实测的可用性结论与实施顺序。
- 已确认生产 HTML 同步预加载四个字体，入口只预加载三个公共包，而首页布局和首页组件继续动态发现；带哈希脚本缓存期仅 4 小时。
- 正在核对源码中是否存在挂载前等待、全局路由守卫和首页组件的同步重依赖，再用真实浏览器量化各项收益。
- 已完成一组冷/热对照：冷会话首次内容绘制 3.64 秒，热刷新 0.524 秒；入口资源下载完后才发现首页相关分块，新增约 0.54 秒瀑布等待。
- 已确认隐藏登录框被首页同步加载并立即请求 3 个认证接口；首页还同步载入三个可视化组件并立即请求模型列表。这些可后移到首次绘制之后或用户操作时。
- 已完成 rn-hybrid 第一轮只读盘点：6 vCPU、15 GiB 内存、500 GB 单虚拟盘、约 445 GB 可用，当前资源空闲；它是 KVM 虚拟机且已有 Nginx、Uvicorn、PostgreSQL 业务，不是专用数据库机。
- 下一步核对该机现有 PostgreSQL 数据量与配置，并实测应用节点到 rn-hybrid、rn01 的延迟；容量足够不等于数据库延迟和可靠性合格。
- 已确认 rn-hybrid 的现有数据库仅约 672 MB，当前业务负载很轻；到主应用机 hd0526 延迟约 1.02 ms，到 OVH 约 24.98 ms，与现有 OVH→rn01 延迟基本相同。
- 已确认入口脚本同步携带两份合计约 644 KB 的完整语言词典；静态分块在 Cloudflare 边缘大量首次未命中，4 小时缓存期会反复制造冷启动。
- 已确认静态服务和两台 Caddy 均未主动设置长期缓存；Cloudflare 的 4 小时缓存来自默认策略。首页容器为空，首屏内容必须等待整条 JavaScript 启动链。
- 已确认 rn-hybrid 在网络容量上适合作为 hd0526 的过渡数据库机，但尚缺系统补丁/重启、防火墙、WireGuard、独立 Niffler 数据库实例和备份恢复验证。
- 已检查现有 R2 备份：昨夜成功，但 rn01 当前剩余空间已经不满足脚本的下次备份前置条件；该风险需要在下一次 04:31 定时任务前处理。
- 取证已足以形成结论，正在整理前端优化优先级和 rn-hybrid 低停机迁移方案；本轮没有修改线上服务。
- 本轮只读评估完成：前端主要问题是空 HTML 外壳、入口携带双语言大词典、根路由二次模块发现、隐藏登录框提前加载，以及静态资源仅缓存 4 小时。
- 最终判断：rn-hybrid 适合作为过渡期 PostgreSQL + Redis 主机，前提是先维护重启、加固网络、部署与现有业务隔离的 PostgreSQL 15/Redis、建立复制和 R2 恢复验证；不建议直接复用现有 PostgreSQL 16 或开放公网数据库端口。
- 本轮未迁移数据库、未修改生产配置、未改前端业务代码；真实浏览器会话已关闭。

---

## 2026-08-14：Niffler 首页卡顿诊断

- 已启动真实浏览器与外部链路诊断；本轮只读，不修改生产或代码。
- 已完成三条首页入口及延迟探针各 5 次外部时序采样，并核对 DNS 和响应头。
- 当前判断：HTML 首包约 0.6–1.0 秒，不足以解释明显的“打开很卡”；Cloudflare 未缓存 HTML、`us1` 与主站/`us2` 版本不一致是已确认异常。
- 浏览器瀑布采集第一次被本地 Chromium 缺失阻断，待切换浏览器运行方式后继续；尚未形成根因结论。
- 已确认可以切换到本机 Google Chrome 继续采集，不需要改动项目或生产环境。
- 已用真实 Chrome 复现：首页首次内容绘制约 10.13 秒，导航约 8.70 秒。
- 已定位两段明显等待：浏览器首次建连约 5.77 秒；5 个首页接口出现约 5.4 秒和 10.3 秒的阶梯式延迟。下一步分别验证 HTTP/3 回退和 API 回源/处理链。
- 已通过 Cloudflare 与直连 API 对照确认接口延迟发生在源站；线上错误直接表明 PostgreSQL 连接池等待超时。
- 当前转入生产只读检查，确认连接池超时的下层原因；HTTP/3 回退属于另一项浏览器入口问题，继续保留但不是接口故障主因。
- 已确认应用机 CPU、内存正常，容器健康检查未覆盖数据库；日志显示首页和模型 API 均以固定 5 秒超时大面积失败。
- 已确认数据库主机 SSH 可达且当前负载偏高，下一步检查 PostgreSQL 进程、连接槽、长事务、锁和系统 I/O。
- 已确认最终根因是数据库服务器根分区 100% 写满：PostgreSQL 因无法写 `postmaster.pid` 持续重启，Redis 持久化也持续失败。
- 继续只读定位空间增长来源并核对故障起点；本轮没有删除文件、重启容器或改生产配置。
- 用户授权优先恢复后，已执行应急处理：清理少量未使用镜像/历史日志；短暂停止 Redis，仅删除 14 个失败的 `temp-*.rdb` 临时快照；保留正式 RDB、AOF 和全部 PostgreSQL 数据。
- 已释放约 22 GB，磁盘由 100% 降至 81%；PostgreSQL 与 Redis 均恢复 healthy。
- 已完成两轮线上接口验证，首页接口恢复 200，模型 API 未认证请求恢复快速 401；下一步确认首页首次绘制和磁盘不会立即再次增长。
- 已用全新 Chrome 会话复测：数据库相关首页接口均降至约 0.30–0.44 秒，入口首次绘制约 2.58 秒；首次内容绘制仍约 7.95 秒，保留为静态资源/前端启动的独立性能问题。
- Redis 第一次恢复后因 5.8 GiB 主机内存不足再次被内核杀死；已新增 6 GiB 应急 swap，并在运行时关闭 Redis RDB 定时快照、保留 AOF。
- Redis 曾在扩容 swap 后的首次加载中再次 OOM，自动重启后当前已 healthy；线上接口仍正常。继续进行连续稳定性观察，不提前宣称完全稳定。
- 已确认持续增长来自 `usage:events`：队列按 2000 条限长，却把完整请求/响应正文写入每条事件，长会话使 Redis live 数据与 AOF 达到数 GB。
- 为继续争取处理时间，已在确认 AOF 正常后删除旧 RDB 和一个失效临时快照，再释放约 4.5 GB；正在寻找线上可用的正文捕获开关，优先停止新增长而不丢计费数字。
- 已更新应急恢复记录 `docs/operations/niffler-disk-full-recovery-2026-08-14.md`，随后将线上 `request_record_level` 从 `full` 改回 `basic`，停止新请求写入完整正文。
- 已暂停并恢复 Background；旧队列 1676 条约 7.08 GB 事件正文精简为约 21.7 MB，计费字段保留，队列最终清空为 `xlen=0/pending=0`。
- 已成功重写 AOF，从约 9.72 GB 降到 MB 以内；恢复自动 AOF 重写，保留 AOF，RDB 定时快照暂时关闭。
- 已移除应急 swap 文件，数据库服务器磁盘稳定在 79%、可用约 20 GB；四个生产容器均 healthy。
- 最终两轮接口和资源验证通过，未再出现固定 5 秒数据库池超时、HTTP 500 或 OOM；应急恢复完成。
- 最终真实 Chrome 验证确认首页 API 已恢复到 0.44–0.53 秒；冷启动首次内容绘制仍约 6.81 秒，记录为后续前端/CDN 性能任务，不与本次数据库事故混淆。


## 2026-08-14 Codex OAuth Compact 与图片任务继承复审

- 用户确认 Compact 和对话内图片应属于当前 Responses 任务，并要求方案无误后直接实施。
- 已核对官方 Codex 客户端：Compact 固定接收一次性 JSON，并携带当前 Responses 的 `session-id` 与 `thread-id`。
- 已核对 Niffler：当前 Compact 的客户端流式标记会直接影响上游 `Accept`；当前无任务标识时使用 API Key 会错误合并独立任务。
- 已将实现边界收紧为只修正官方 Codex OAuth Compact，不改变其他兼容 Provider。
- 已更新正式设计记录：父任务携带原 `thread-id` 并继承；独立请求生成请求级任务编号；回合优先映射客户端 `turn_id`；缓存身份使用账号级 `session_id`；官方 Codex OAuth Compact 上游固定普通 JSON。
- 当前开始修改代码和测试。
- 编译前检查发现管理端 Compact `Accept` 补丁首次落到同文件的图片测试链；已移除并改到正确的标准 Responses/Compact 测试链，尚未运行错误代码。
- 已完成任务与回合继承：同一 Responses 任务的普通请求、Compact 和图片请求使用同一映射后任务编号；没有任务信号的独立请求各自生成任务编号，不再按 Niffler API Key 永久合并。
- 已将账号缓存身份改为稳定会话编号，任务编号只用于区分同一账号下的并行任务；普通请求、Compact 和图片测试断言已同步。
- 官方 Codex OAuth Compact 现在无条件删除请求正文中的 `stream`，并固定发送 `Accept: application/json`；该协议规则不受身份收敛开关影响，也不改变自定义或非 OAuth Compact Provider。
- Compact 完整 JSON 转客户端流时，会为每个压缩结果发送 `response.output_item.done`，最后发送 `response.completed`；缺少 `output` 的无效响应会明确失败，不再静默丢失结果。
- 已修正分块 JSON 处理：即使上游没有 `Content-Length`，Niffler 也会先收完 Compact 响应再转换，不会将 JSON 分块直接当作 SSE 转发。
- 首次完整网关测试发现两处格式判断绕过网关统一接口；已修正模块边界后重跑，3004 项全部通过。
- 最终验证通过：`aether-ai-formats` 383 项、`aether-provider-transport` 259 项、网关 3004 项；身份收敛 19 项、分块 Compact 回归、普通 Responses 和图片实际请求链均通过；三包严格 Clippy、Rust 格式和差异检查通过。
- 本轮未提交、未发布，也未修改运行中的全局开关；工作区其他已有改动保持不变。

## 2026-08-14 Codex OAuth 客户端环境身份一致性

- 已在身份收敛最终阶段删除客户端 SDK、浏览器、应用、语言、TLS、操作系统和架构请求头；处理范围仅限开关开启后的 Codex OAuth Responses、Responses Compact 和图片桥接请求。
- 已保留请求正文、工具定义、`x-codex-turn-metadata` 中的沙箱和工作区字段，以及 `x-business-context` 这类不属于环境身份的自定义请求头。
- 已补强开关关闭和非 Codex 请求测试：原始设备证明与客户端环境请求头均保持不变，证明删除不会越过功能边界。
- 验证通过：身份收敛测试 16 项、普通 Responses 实际流式请求、管理端 Responses 模型测试、管理端图片模型测试、正式图片生成请求、严格 Clippy、Rust 格式检查和差异检查。
- 本轮未提交、未部署；工作区原有其他改动保持不变。

## 2026-08-14 Codex OAuth Compact 与图片身份收敛

- 已核对三个请求入口：普通 Responses 已执行收敛；Compact 被适用范围排除；独立图片请求没有调用最终收敛函数。
- 已更新架构设计：Compact 收敛身份请求头和缓存键、保留自己的请求 ID且不增加 `client_metadata`；图片桥接执行普通 Responses 的完整收敛；三类请求都删除客户端设备证明。
- 已扩展收敛核心：普通 Responses 和图片执行完整正文与请求头收敛；Compact 收敛请求头和缓存键、保留本次压缩请求编号，并删除不属于官方 Compact 正文的 `client_metadata`。
- 已接入正式图片专用请求链和管理端“测试模型”图片链；两条链都在全部请求规则完成后最后执行收敛。
- 已补充单元、架构、正式图片请求和管理端图片测试，验证三类请求删除客户端设备证明，图片工具正文和 Compact 压缩历史保持不变。
- 验证通过：身份收敛定向测试 16 项、图片正式请求测试、管理端图片测试、图片终态顺序架构测试、严格 Clippy、Rust 格式检查和差异检查。
- 本轮未提交、未部署；工作区原有其他改动保持不变。

---

## 2026-08-13 Niffler 服务器采购计划

- 已核对 rn01、hd0526 和 OVH VPS 的实时 CPU、内存、磁盘、容器和网络状态，没有修改生产环境。
- 已确认数据库约 25 GB、Redis 使用约 2.07 GB且没有内存上限，R2 自动备份最近一次状态成功。
- 已核对 OVH、GorillaServers、FiberState、Bandwagon 和 Cloudflare R2 的 2026-08-13 官方价格、规格或库存页面。
- 已按“用户量增加 200%”采用当前规模的 3 倍重新计算：951 名注册用户、288 名月活请求用户、约 376 万次请求/月。
- 已将图片生成、视频生成、剪辑、字幕合成和 FFmpeg 转码纳入正式架构；工作台独立发布、复用 Niffler 登录和模型接口，媒体文件进入 R2。
- 已更新 `docs/operations/server-procurement-plan-2026-08-13.md`：当前先采购 64 GB OVH 主数据库物理机，扩大大陆用户前采购搬瓦工三网优化入口，工作台公开上线前采购 OVH 新加坡 FFmpeg 媒体处理机，达到触发条件前替换 rn01。
- 已纠正原先的容量判断：双 960 GB RAID1 在 3 倍线性增长且不清理历史记录时可能在一年左右接近警戒线，因此首选 2×1.92 TB RAID1 或 4×960 GB RAID10，并要求实施数据保留和归档。
- 文档已经覆盖 3 倍容量依据、工作台边界、目标架构、故障切换、域名代理边界、数据库/Redis、R2 容量成本、采购预算、实施顺序和验收清单。
- 已增加亚太和中国大陆网络方案：大陆使用搬瓦工洛杉矶三网优化入口，亚太其他地区先使用 Cloudflare，不增加跨洲连接美西数据库的亚太 Frontdoor。
- 已根据媒体地域重新调整采购：FFmpeg Worker 从 GorillaServers 洛杉矶改为 OVH 新加坡，使用 R2 亚太位置提示、Local Uploads 和媒体分发域名；大陆大文件实测不合格时才增加阿里云香港 OSS。
- 已明确所有新增域名均待用户确认，文档没有创建域名或改变任何现有橙云/灰云状态。
- 本轮未购买服务器，未修改 DNS、生产配置、数据库、Redis、备份任务或运行服务。

---

## 2026-08-12 Niffler Codex OAuth 设备指纹收敛方案

- **2026-08-13 Review Fix:** 用户同意修复审查后保留的四项问题。已确认不支持非官方 `thread_id` HTTP 头，不改完整请求记录。当前先更新设计记录，再实施和验证。
- 已先更新正式设计记录：新增客户端身份三元组强制统一、父子任务同命名空间映射、模型测试覆盖和 Codex 配置故障隔离要求。
- 已完成核心模块和四条正式请求调用链：客户端三项版本标识统一；父任务、分支来源任务稳定映射；开关延迟到确认 Codex OAuth Responses 后读取。
- `cargo test -p aether-gateway codex_identity_convergence --no-fail-fast` 通过，9 项测试全部成功。
- 管理端标准模型测试现已在请求真正发出前复用同一个收敛处理器；首次补丁位置错误已在编译前纠正，`cargo check -p aether-gateway` 通过。
- 新增非 Codex 故障隔离测试后，核心定向测试 10 项全部通过。管理端集成测试首次运行时身份断言全部通过，随后因旧的 `input` 数组形状断言失败；该断言与身份功能无关，正在改为内容校验。
- 管理端 Codex OAuth 模型测试集成测试已通过。请求级上下文现在还固定本次请求使用的 Codex 客户端版本，避免同一次请求重试时版本状态变化造成身份不一致。
- sub2api 解析与完整导入回归均通过。严格 Clippy 首次运行只发现一处多余取引用，已修正并准备重跑。
- 最终复核确认还应覆盖普通用户真实请求链，现正扩展已有 Codex OAuth 流式端到端测试，验证开关开启后的最终出站身份。
- 普通用户 Codex OAuth 流式端到端测试已通过，确认正式请求会统一安装、会话、任务、缓存键和客户端版本身份。
- 最终 `cargo clippy -p aether-gateway --all-targets -- -D warnings`、`git diff --check`、`cargo fmt --all -- --check` 全部通过；本轮四项修复完成，未提交、未部署。
- **2026-08-13 Release:** 用户要求提交到远端 main 并上线。已确认远端 `main` 比当前工作分支新 16 个提交，仓库强制使用“功能分支 → test → main → 手动构建准确镜像 → 生产部署”发布链；当前先整理不含其他工作区改动的精确提交。
- 已完成精确暂存；中英文多语言文件仅暂存“Provider 高级设置”、身份收敛配置及保存提示，套餐、首页测速、部署配置和计划记录等其他工作区改动均未进入提交。
- 发布前验证通过：核心收敛单元测试 12 项、管理端模型测试、sub2api 导入、普通用户 Codex OAuth 流式请求、前端组件测试 3 项、前端类型检查、严格 Clippy、Rust 格式检查、差异检查和前端生产构建均成功。
- 已创建规范提交 `1722b4a8b`（`feat(codex): 增加 OAuth 身份收敛`），提交只包含本功能的 25 个文件。
- 已从最新 `origin/main` 创建干净发布分支并无冲突移植为提交 `7134bb739`；重新执行核心测试、严格 Clippy、前端组件测试、类型检查、生产构建、格式和差异检查，全部通过。
- 干净工作区执行 `npm ci` 时报告仓库现有依赖有 2 个高危、2 个严重漏洞；本次没有修改依赖锁文件，该风险不阻断当前功能验证，保留在发布结果中。
- test 合并请求 #37 的 14 项检查全部通过，已使用合并提交 `6ca4a7fbc` 进入 `test`；对应 Build App Image 工作流成功构建前端、网关和镜像，并成功部署 mylingweave 测试环境。
- main 晋级合并请求 #38 的 20 项检查全部通过，发布策略确认准确 test 提交已完成测试部署；已按仓库既有管理员流程合并到 `main`，主线提交为 `e1edf18d9481823422ee8608faf4b54f1402af07`。
- 准确 main 提交的 Build App Image 运行 31672661254 成功，前端、Linux 网关和 amd64 镜像产物全部完成。
- Deploy Production 运行 31673279709 成功；生产前台和后台容器均运行镜像 `niffler-app:e1edf18d9481823422ee8608faf4b54f1402af07` 并处于 healthy。
- `https://niffler.org/_gateway/health` 与 `https://api.niffler.org/_gateway/health` 已间隔 5 秒完成两轮外部检查，均返回 HTTP 200 和 `status: ok`。
- 生产功能代码已上线；系统级身份收敛开关默认关闭，本次发布没有擅自修改生产运行配置。

- **Status:** implemented and verified
- **Phase:** Implementation Phase 4
- **Actions taken:**
  - 明确本轮角色为后端架构与安全评审，只分析、不实现。
  - 启用 `planning-with-files`，建立当前任务的目标、边界、关键问题和调查阶段。
  - 检查工作区状态，确认已有未提交改动和既有计划文件，本轮只追加调查记录。
  - 两次补丁因标题上下文假设不精确未应用；读取真实标题后完成追加。
  - 确认本机有 sub2api 源码及 v0.1.175 标签，冻结参照提交和只读取证方式。
  - 从标签说明确认该版本由 PR #5553 引入四档指纹收敛策略；尚未读取实现细节。
  - 完整读取 v0.1.175 的后端实现、网关接入差异、测试目录和账号配置界面差异。
  - 确认该功能不改变 OAuth 授权/刷新流程，也不增加数据库字段；核心是 OpenAI OAuth 账号级请求标识改写。
  - 完成 Niffler 第一轮路径定位，确认实现会跨请求格式、计划/发送、Provider 账号数据与管理界面多个模块。
  - 排除站内用户登录设备 ID 与 Codex 上游设备指纹的同名干扰。
  - 发现 Niffler 已按用户 API Key 生成稳定 `prompt_cache_key/session_id/conversation_id`，但并非上游账号级收敛。
  - 发现现有实现只在客户端未提供标识时补默认值，客户端标识仍会原样到达上游。
  - 追踪 Provider 请求计划数据，确认模式配置和账号稳定种子可在计划生成时一次性取得。
  - 确认 OAuth `auth_config` 属于加密且不回显的凭证载荷，不适合作为管理界面的普通配置存储位置。
  - 确认 Provider Key 已有跨三种数据库和导入导出的 `fingerprint` JSON 配置，可承载 Codex 模式而不做数据迁移。
  - 定位现有 Codex 函数签名缺口：当前没有 Provider Key ID 与指纹配置输入。
  - 确认 Provider Key `fingerprint` 已进入每个候选的不可变传输快照，重试换账号时可自然切换指纹。
  - 识别跨实例迁移风险：只按本地 Provider Key ID 派生会在导入后改变上游设备身份。
  - 枚举 Codex 体和头处理的全部生产调用点，确认存在多条标准与转换入口。
  - 初步确定终态处理器方案：在选中账号、规则和格式转换完成后统一改写，不侵入通用转换函数。
  - 核对 sub2api 实际覆盖范围，确认主 Responses HTTP 路径生效、compact 明确跳过，其他直达构造路径未随 PR 接入。
  - 核对 Niffler 的 sub2api OAuth 导入契约，确认目前不会迁移 `openai_device_id` 和指纹模式。
  - 核对 Provider Key 新建、更新、OAuth 创建和重复导入逻辑，确认 `fingerprint` 可复用但需要命名空间校验与合并。
  - 定位管理界面入口为 OAuth 账号编辑对话框，并识别必须保留其他指纹子配置。
  - 确认 Codex 标识当前会经同格式和跨格式头复制原样到达上游。
  - 固定终态处理顺序：所有普通头规则完成后再改写指纹字段。
  - 通过本机 OpenAI Codex 官方源码核对标识载体，确认头、`client_metadata` 和内嵌 turn metadata 的字段关系。
  - 识别官方新增父线程字段与 compact 现状，并将其列为明确边界。
  - 从官方元数据模型确认内嵌 turn metadata 是权威数据，规划只改身份字段并保留子代理与工作区元数据。
  - 确认前端复用组件缺少 Provider 类型，方案中需要由两个调用入口显式传入并只对 Codex OAuth 显示。
  - 核对现有稳定缓存 ID 的历史语义和 Rust UUID 能力，确认缓存继续按用户隔离、请求级 UUIDv7 需要增加依赖特性。
  - 初步冻结账号级 ID 派生方案与可选外部 installation ID 边界。
  - 确认 Niffler 当前没有 Responses WebSocket 出站路径，compact 是需要明确跳过的真实 HTTP 路径。
  - 确认项目没有 OAuth 账号批量编辑能力，第一版不为此功能额外扩展批量 API。
  - 确认 sub2api v0.1.175 账户导出会包含完整 `Extra`，Niffler 可以从导出文件白名单迁移模式与安装 ID。
  - 已形成数据结构、适用范围、终态改写顺序、兼容边界和验证矩阵草案；尚未修改任何业务代码。
  - 完成方案复核：复用现有 `fingerprint` JSON，使用持久 seed 保证跨节点与导入导出稳定。
  - 完成本轮交付准备；仅更新调查记录，没有修改业务代码、数据库、配置、凭证或部署状态。
  - 根据用户追问重新核对代码并纠正两处表述：参照 sub2api 时缺少模式应默认 `session`，现有账号会生效；`conversation_id` 不是该版本收敛字段，也不是官方 Codex Responses 出站头，不能直接断言应等于 `thread_id`。
  - 用户要求全面评审整个方案；当前重新进入 Review Phase 1，不沿用未重新证明的设计结论，仍不修改业务代码。
  - Review Phase 1 发现 seed 方案与旧账号默认启用存在结构性矛盾；确认 Provider Key UUID 已由完整系统导入导出保留，初步裁决删除额外 seed，改按 Key ID 确定性派生。
  - 完成主要请求入口复核：终态处理必须同时接收最终 body/headers，并在候选计划返回前调用；不能依赖现有分离的 Codex 头体 helper。
  - 核对官方元数据语义：不凭空生成带虚假请求类型的 turn metadata，只改写客户端已有的元数据并补官方扁平兼容字段。
  - 重读 sub2api 出站头顺序，确认 v0.1.175 的指纹功能不删除或覆盖既有 `conversation_id`；撤销上一轮“session/full 删除 conversation_id”的建议，改为首版保持现状并单列风险。
  - 识别跨格式线程碰撞：只读取 Codex session 头会把无该头的 Chat/Claude/Gemini 请求合成一个 thread；方案改为复用 Niffler 已解析的会话亲和信号，并以用户 API Key ID 作最后稳定回退。
  - 发现原生同格式 Responses 会走独立 `LocalSameFormatProvider` 路径，之前的入口清单漏了最常见路径；最终方案增加该出口和管理端模型测试路径。
  - 复核管理接口和前端类型：后端已返回 fingerprint，前端只缺类型；更新为整对象替换，需要深合并和后端命名空间校验。
  - 发现号池现有 regenerate_fingerprint 会整对象覆盖，可能破坏 Codex 配置；列为实施前必须修正的阻断风险。
  - 核对最终请求策略顺序，确认路由策略和受管理提示词会在请求构造后继续修改上游头/正文；收敛必须成为 `AiExecutionDecision` 返回前的最后一次修改，不能放在更早的请求 builder 中。
  - 核对传输快照和现有 OAuth 判定语义，确认可直接按 Provider 类型、Key 认证类型和 Endpoint API 格式精确限定功能，无需执行阶段查库。
  - 复核默认值与上线方案，确认“旧账号缺失即 session”与“少量账号先启用”互相冲突；最终方案将账号模式与全局发布开关分离，并用批量模式操作支撑分组上线和紧急回退。
  - 冻结导入覆盖和异常元数据方向：sub2api 仅迁移 Codex 白名单字段；损坏 metadata 删除该载体并继续扁平字段，不让客户端坏 JSON 中断整个请求。
  - 重新核对客户端会话识别，发现 Codex 适配器漏读标准 `session-id`；最终 thread 方案改为修正统一 affinity 解析后复用结果，不再维护第二套字段优先级。
  - 纠正重要事实：Niffler 系统配置导出不会保留 Provider Key ID，配置导入会生成新 ID；删除 seed 的理由改为源语义与范围控制，并明确第一版不承诺账号重建后的 session/thread 不变。
  - 核对 OAuth 401/403 重试和管理端模型测试：同账号刷新只替换认证头并原计划重放，指纹应保持；模型测试直接构造 ExecutionPlan，需要显式调用同一纯收敛核心。
  - 核对系统配置导入，确认其可绕过普通 Key API 校验；Codex fingerprint 规范化必须成为新建、更新、系统导入、OAuth 导入和批量操作共用的后端函数。
  - 核对号池批量接口与系统设置缓存，确认可用现有选中/全筛选结果和 batch payload 增加模式设置，并用现有 3 秒系统配置缓存实现总开关而不增加每请求数据库查询。
  - 冻结最终最小配置和四档字段契约：只保存 mode/可选 installation ID，conversation_id 与 prompt_cache_key 保持现状，六类头体载体使用同一请求级 ID 集合。
  - 审查诊断脱敏，发现 Codex 会话/线程/设备头目前会明文进入请求 trace、runtime miss 和模型测试摘要；列为实施前必须修正的安全阻断项。
  - 完成重复导入复核：批量导入默认拒绝覆盖活动重复账号，只有现有规则认定可替换的账号才更新；单条导入需明确 `replace_existing=true` 才能覆盖活动账号。
  - 最终否决“历史账号缺少配置即 session”的 Niffler 本地默认值：历史账号缺少配置时保持旧行为，新建账号和 sub2api 导入显式写 session，总开关只负责统一启停和紧急回退。
  - 完成失败原子性、请求内开关一致性、大小写重复头、ASCII Header JSON、元数据大小边界、性能和多节点稳定范围复核。
  - 完成全面评审并冻结修订方案、实施阻断项、测试矩阵、分阶段上线和三层回退；没有修改业务代码、数据库、运行配置或部署。
  - 最终补查官方 compact 请求，确认其会发送 installation/session/thread/window 与 compaction turn metadata；将 sub2api v0.1.175 漏掉的 compact 纳入 Niffler 首版，但只改头部，不向 compact 正文增加字段。
  - 发现前端整对象深合并仍不能解决并发丢更新；最终方案要求服务端原子更新 `fingerprint.codex`，并让 OAuth Token 刷新只更新凭证相关列，不再用整行写回触碰 fingerprint。
  - 核对当前官方窗口语义，确认窗口编号会在压缩后递增；不再照抄 sub2api 固定 `thread:0`，改为保留客户端合法窗口编号并只替换 thread 部分。
  - 用户明确要求同一 OAuth 账号被多人共用时尽量呈现为一个人使用；据此推翻此前“旧账号缺少模式按 off”和“保留用户级 conversation_id”两项不符合目标的裁决。
  - 重新核对 sub2api v0.1.175，确认其确实转发 `conversation_id`，但该值来自既有的 API Key 隔离逻辑，新指纹功能不改它；因此参考版本仍留下用户级差异。
  - 重新核对官方 Codex 源码，确认 `conversation_id` 在应用内部常用作旧的线程命名，但上游 Responses 请求使用 `thread-id`，不要求 `conversation_id` HTTP 头。
  - 最终方案规定非 off 模式删除出站 `conversation_id`，不再将它等同于 thread；原值只可在改写前作为内部任务识别输入。
  - 发现 Niffler 的 `prompt_cache_key` 同样按平台用户 API Key 生成；官方 Codex 默认使用当前 thread ID 作为缓存键。最终方案改为 session/full 使用收敛后的 thread ID，消除第二个稳定用户标签。
  - 按用户要求再次逐项复审已交付方案，并直接读取 sub2api v0.1.175 指纹实现、测试、Niffler 当前 Codex 头体构造和本机官方 Codex 请求契约。
  - 确认已交付方案的一处明确错误：`x-client-request-id` 应等于收敛后的 thread，而不是每请求随机；每请求随机的是 `turn_id`。
  - 收紧第一版范围为普通 Codex Responses HTTP；sub2api v0.1.175 明确跳过 compact，WebSocket 与图片也不纳入首版对标承诺。
  - 修订账号派生、窗口编号、配置深合并、失败行为、设备证明、客户端身份头和网络出口的边界，形成可进入文档设计阶段的复审结论。
  - 本轮仍只更新调查记录，没有修改业务代码、数据库、配置、凭证或部署状态。
  - 将父线程和 fork ID 纳入同一账号级线程映射，统一 OAuth account ID 与 Codex 客户端身份头，并把固定网络出口和设备证明兼容列为明确上线条件。
  - 最终确认旧账号缺少配置只表示没有新字段，不影响 OAuth 有效性；总开关开启后缺失模式按 session，全部有效旧账号自动生效，只有显式 off 才关闭。
  - 完成重新评审和方案记录；仍未修改业务代码、数据库、运行配置、OAuth 凭证或部署。
  - 用户最终确认配置形态改为系统级统一开关：关闭时全部 Codex OAuth 账号保持现状，开启时全部现有及以后新增账号统一使用 session；取消账号级、批量和 Provider 级配置入口。
  - 明确全局统一只统一模式和启停，不会让不同 OAuth 账号共用同一个设备身份；每个账号仍自动生成自己的稳定身份。
  - 已同步方案记录，未修改业务代码、数据库、运行配置、OAuth 凭证或部署。
  - 用户确认继续实施后，先新增正式设计记录，再完成系统级布尔开关；默认关闭，旧账号不需要补配置或重新授权，开启后立即统一生效。
  - 网关在每次入站请求只读取一次开关和生成一次回合标识；同一逻辑请求的全部账号尝试和重试共享该标识。
  - 普通 Codex OAuth Responses 请求在所有请求规则完成后统一改写设备、会话、任务、回合、窗口和缓存标识，并删除用户级旧标识及客户端设备证明。
  - 每个 OAuth 账号使用独立且稳定的设备和会话身份；同一账号内按原始任务信号生成稳定任务身份，不同任务仍可并行。
  - sub2api 批量导入只迁移合法 `extra.openai_device_id`，忽略源模式和任意扩展字段；更新已有账号时保留其他 fingerprint 配置。
  - 系统设置新增“Provider 高级设置”页签和唯一开关，覆盖加载、加载失败、未修改、保存中、保存成功和保存失败状态；账号编辑页没有新增配置。
  - Codex 身份请求头加入强制脱敏清单；回合元数据请求头使用纯 ASCII JSON，避免中文等内容形成非法 HTTP 请求头。
  - 相关 Rust 单元、集成和架构测试通过，网关严格 Clippy 通过；前端组件测试、类型检查和生产构建通过；UI 最终审查输出 `PASS ui-review gate`。
  - 本轮未提交、未部署，也未修改运行中的全局开关。
- **Files created/modified:**
  - `docs/architecture/codex-oauth-identity-convergence.md`（正式设计记录）
  - `apps/aether-gateway`（请求级身份上下文、最终出站改写、sub2api 导入及测试）
  - `crates/aether-admin`、`crates/aether-usage-runtime`（系统配置与日志脱敏）
  - `frontend/src/views/admin/system-settings`、`frontend/src/i18n/locales`（统一开关、状态与文案）
  - `task_plan.md`、`findings.md`、`progress.md`（实施记录）

---

## 2026-08-09 有限钱包持续透支严重计费缺陷审查

- 已完成本地根治实现：钱包余额小于等于 0 时不再授权钱包付费，适用套餐仍可使用；只有余额大于 0 时开始的最后一批钱包请求可以在结算后产生欠费；套餐不足部分按同一准入继续扣钱包，套餐额度只扣到 0。
- 请求准入、请求记录和结算已使用不可变的请求级计费记录；套餐供应商范围按权益保存，供应商范围内重试不会串扣其他权益。
- 套餐模板改为选择供应商，可用模型由供应商当前有效模型动态决定；新增模型无需再次编辑套餐模型清单。
- 负钱包不能创建套餐订单，支付发放时再次复核；同一用户同一时刻只允许一个生效套餐，同套餐续费顺延，其他套餐重叠或不同套餐待支付订单会被拒绝。
- 管理端用户列表、钱包详情和用户钱包中心已显示真实有符号余额；用户列表改为当前页批量读取钱包摘要，不再由前端全量读取钱包。
- 已删除套餐管理页的“是否允许钱包补差”开关；新保存的套餐固定执行“套餐扣到 0，超出部分扣钱包”，旧配置只为数据兼容保留，不再影响准入和结算。
- 最终验证通过：`aether-data` 519 项、`aether-data-contracts` 45 项、`aether-wallet` 5 项、`aether-gateway` 2978 项全部通过；三项目严格 Clippy、Rust 格式和差异检查通过。前端类型检查、目标 ESLint、套餐供应商测试、生产构建和 UI 复审通过；构建只有既有的浏览器数据过期和大分块警告。
- 本轮未提交、未部署、未修改生产钱包。生产只读名单仍为 44 个负钱包用户；修复上线并确认异常扣款停止后才能按审计流水补偿。

- 最终一致性评审发现套餐结算仍需补充：现有严格套餐在实际费用超过剩余额度时可能整笔不扣，钱包补差套餐则会继续扣负钱包；最终设计必须按请求准入时的资金来源结算。由套餐合法准入的最后一批请求先将套餐扣到 0，超出部分继续扣钱包，即使钱包原本已负；结算完成后，没有其他适用套餐的新请求才被拒绝。
- 已发现当前套餐可用性存在 3 秒 Redis 缓存且结算后不准确失效，会在套餐耗尽后继续放行新请求；最终资金授权必须读取 PostgreSQL 已提交状态，供应商模型目录才允许缓存。
- 已确认通用请求元数据会被过滤和截断，不能直接作为资金授权凭证；准入决定需要专用服务端字段或表，并在调用上游前提交成功。
- 已确认现有 pending 使用记录通过后台任务异步写入且失败时只告警，不能承载资金准入凭证；需要复用上游前同步记录，或增加一次同步写入并重新测量延迟。
- 已找到可复用的同步写入点：同步和流式请求都在上游前等待请求候选记录；可把请求级计费准入和候选写入放进同一事务，但有限计费请求必须把当前失败后继续执行改成明确失败。
- 已确认供应商成为套餐正式关系后不能继续只存 JSON；需要套餐供应商和已购权益供应商关系表，并阻止删除仍被有效权益引用的供应商。
- 已发现用户可用模型接口目前只看分组供应商，不会合并套餐供应商；套餐改造必须同步模型目录和结构化错误，否则会出现“套餐能用但模型列表看不到”或把供应商故障误报成欠费。
- 已完成最终一致性评审：补充按实际供应商形成资金方案、套餐候选优先、供应商故障不静默扣钱包、多套餐固定消耗顺序，以及无预占下只能限制最后一批数量而不能保证美元金额上限的剩余风险。
- 用户最终确定套餐最后一批结算语义：套餐只扣到 0，超出部分继续扣钱包，即使钱包原本已负；结算完成后没有其他适用套餐的新请求停止。删除独立套餐超用金额设计。
- 用户新增欠费购买规则：钱包真实余额小于 0 时禁止购买、续购和自动续费套餐；已有套餐继续使用，清偿至 0 或正数后恢复购买。
- 用户确认最终方案没有问题即可执行。已完成最终一致性复审并新增 `docs/architecture/billing-overdraft-root-fix.md`，明确目标、非目标、资金规则、套餐供应商关系、请求级准入、购买双重校验、兼容迁移、性能要求和验证方式；开始本地实现，不直接修改生产数据或服务。
- Phase 13 已先补迁移契约失败测试，再新增 PostgreSQL、MySQL、SQLite 的 `20260809130000_add_billing_admissions_and_plan_providers.sql`；目标测试通过（1 passed），开始数据契约和仓库事务改造。
- 套餐与已购权益的数据契约已增加供应商 ID；内存、PostgreSQL、MySQL、SQLite 套餐仓库均可在同一事务中保存供应商关系，数据层编译和测试编译通过。
- 已先增加负钱包购买套餐的失败测试，再在三种数据库和内存实现中拒绝用户购买；管理员明确赠送套餐继续允许，目标测试通过。支付回调发放时的第二次余额校验仍在实现中。
- 套餐发放现在会在同一事务中保存供应商快照；SQLite 集成测试验证了供应商关系和管理员赠送额度。普通购买在支付回调或人工确认发放前会再次读取真实钱包余额，负数时停止发放；管理员赠送继续允许。5 项 SQLite 套餐回归通过。
- 请求级计费准入已加入候选写入仓库；PostgreSQL、MySQL、SQLite 会在同一事务中写准入和上游请求记录，有限计费请求可以据此改为写入失败即停止。SQLite 原子写入集成测试、数据层和网关编译通过。
- 用户决定历史问题不重放、不追缴，只列出受影响负钱包用户并通过审计调整补到 0，额外使用视为赠送；本轮不直接修改生产钱包。
- 2026-08-09 13:02（北京时间）再次读取生产库：44 个有效有限钱包为负，合计 2454.090266 USD；41 个有请求结算扣成负数的直接证据，合计 2454.031614 USD；3 个迁移钱包没有同类证据，单独待核对。此前记录的 45 个属于更早动态快照，不能作为最终补偿名单。
- 已纠正“负钱包拒绝所有请求”的过严规则：负钱包只禁用钱包支付，适用且未耗尽的已购套餐继续可用；无关套餐不能再错误放行请求。
- 用户补充指出套餐应选择可用供应商，由供应商动态决定可用模型；当前已确认套餐只保存静态模型 ID，“按供应商快速选择”也只是一次性复制，因此供应商增加 `gpt-5.6-sol` 后旧套餐不会自动生效。
- 已形成套餐范围修正：套餐与已购权益保存供应商 ID，模型范围按当前有效供应商模型关系动态派生；准入、候选路由和结算使用同一请求级决定，供应商新增模型无需批量修改套餐。
- 用户补充确认用户列表的钱包列会把真实负余额显示成 0，并要求从需求、架构、代码、体验和延迟全面复盘；当前进入扩展事故审查。
- 用户确认取消金额预占，接受余额或套餐额度耗尽前已开始的最后一批请求产生超用；严格限制为一笔会要求串行或预占，因此最终规则是不改变正常并发，负钱包只停止钱包支付，已购套餐继续独立判断。
- 已确认用户列表负余额显示为 0 的直接原因：管理端钱包摘要主动对 `wallet_balance` 和 `total_available_balance` 截断，用户列表优先使用这些派生字段；现有负值红色样式因此无法生效。
- 已核对现有用户和 API Key 并发限制：它通过读取最近 128 条请求后内存计数，没有将检查和占用合并为跨 Frontdoor 原子操作，不能保证余额耗尽时只有一个最后请求。
- 已确认用户列表还存在查询放大：用户接口已读取钱包但不返回，前端再次拉取全部钱包，后端再逐个查询套餐；整改可同时修正余额真实性和管理页性能。
- 已明确不新增 Redis 用户许可、租约续期或并发限制，不改变 Codex、多标签页和并行任务的正常使用方式。
- 已明确无金额预占下的产品边界：允许最后一批已进入上游的请求按真实费用完成结算；钱包欠费后不再授权钱包请求，但适用且未耗尽的套餐仍可独立授权。
- 已确认负余额隐藏覆盖管理端用户列表、用户钱包中心和首页余额摘要；页面仍显示原始充值余额负数和“正常”状态，造成同屏口径冲突。
- 已确认现有测试显式要求有限钱包不足时透支并结算，说明事故来源包含错误需求被编码为预期行为，不能只补一个条件判断。
- 已纠正前一版过度设计：删除服务端单请求硬上限、Redis 用户活动许可和计费租约；最终方案采用供应商范围统一准入、请求准入证据、PostgreSQL 真实结算和负钱包停止钱包支付。
- 已完成管理端和用户端统一展示方案：真实余额不截断，欠费与启用状态分离，套餐不再抵消欠费，用户列表改为当前页批量读取。
- 已完成请求延迟、管理列表性能、历史数据修复、跨节点并发、充值恢复、告警和验收测试设计；本轮仍只审查，没有修改业务代码或生产数据。
- 已完成生产用户、钱包、API Key、套餐权益、额度窗口、请求记录和结算快照的只读核账。
- 已确认生产镜像提交与本地审查文件内容一致，结论对应当前线上实现。
- 已定位请求前估价失败时直接放行、结算把钱包存在误作允许透支、有限钱包扣款产生充值余额透支三处直接代码原因。
- 已确认初步方案“禁用 Key + 零余额拒绝 + 禁止负数”只能止损，尚不足以解决并发、最终费用偏差和欠费记账；正在形成完整系统修复方案。
- 已审查现有真实预占：仅单进程串行、只预占钱包、估价失败跳过、固定 30 分钟过期，并与旧结算分开完成，不适合作为双 Frontdoor 的生产资金控制点。
- 已确认模型 `output_limit` 没有进入请求前估价或统一上游强制限制，Codex Responses 默认还会删除客户端输出上限；当前费用估算不能称为可执行上界。
- 已确认请求前简化估价没有复用正式计费引擎，遗漏缓存、图片等费用维度，输入 Token 的“字符数除以 4”也不是保守上界。
- 已完成统一准入、结算授权、异常欠费状态、历史数据修复、灰度上线和监控验证的完整方案审查。
- 最终判断为 P0 系统性资金正确性事故；现有预占实现不是事故根因，也不应作为本次根治方案启用。
- 本轮未修改生产数据、配置、API Key 或服务，也未修改业务代码。

## 2026-08-09 Niffler 美西双入口与用户侧延迟显示

- 用户确认旧应用节点为 hd0526，不是误写的 hd0527。
- 切换前确认 `api.niffler.org` 指向 OVH 并开启 Cloudflare 代理，`us1` 和 `us2` 尚未创建。
- 已确认 OVH 与 hd0526 的 Frontdoor、Caddy 均正常，OVH 不运行 Background，hd0526 继续运行唯一 Background。
- 已确认直连 OVH 需要同时调整 Caddy 证书策略和防火墙；尚未修改 DNS、生产配置或前端代码。
- 已完成性能对比：OVH 健康接口约 1 毫秒，但涉及数据库的公开模型列表为 108 至 168 毫秒；hd0526 的相同预热请求为 8 至 12 毫秒。
- 已确认主要差异来自应用到 rn01 的网络：OVH 约 25 毫秒，hd0526 约 0.75 毫秒。用户反馈的迁移后变慢属实。
- 已创建 `us1.niffler.org` 和 `us2.niffler.org`，并关闭 `api.niffler.org` 的 Cloudflare 代理。曾错误地将 `us1`、`us2` 设为灰云，现已纠正为橙云；只有 `api` 保持灰云直连。
- 第一阶段验证时，1.1.1.1 与 8.8.8.8 均确认 `us1`、`us2` 返回 Cloudflare 地址；两条测速请求的响应服务器均为 Cloudflare，回源后返回 204。当时 `api` 仍直接解析到 OVH，随后已按最终要求切到 hd0526。
- OVH 已开放公开 80/443，取得公开可信证书并切换到 Caddy 自动续证；hd0526 已取得 `us2` 公开证书。两台配置均已保存 `0600` 回退副本。
- `us1`、`us2` 测速接口返回 204，`api` 健康接口返回 200；OVH 只运行健康的 Frontdoor，hd0526 继续运行健康的 Frontdoor 和唯一 Background。
- `api`、`us1`、`us2` 的健康接口、公开认证设置和公开模型列表均返回 200，确认两台 Frontdoor 都能读取共享数据库与 Redis。
- 首页测速组件已实现，前端类型检查、目标 ESLint、相关 9 项测试和生产构建通过；代码尚未提交或按 `main` 的 CI 镜像发布。
- 已将 `api.niffler.org` 改为灰云直连 hd0526，将 `niffler.org` 保持橙云并回源 hd0526，保留 `us1` 橙云回源 OVH、`us2` 橙云回源 hd0526，并删除 `hub.niffler.org`、`cf.niffler.org`。
- hd0526 Caddy 只保留 `api`、根域名、`us2` 和原有独立站点；OVH Caddy 只保留 `us1`、内部源站地址和原有汽车站点。两台配置均已校验并保存权限为 `0600` 的回退副本。
- 1.1.1.1 与 8.8.8.8 最终确认根域名、`us1`、`us2` 返回 Cloudflare 地址，`api` 解析到 `23.19.228.223`，`hub`、`cf` 不存在；主页、InfiniteCanvas、健康、认证设置和公开模型接口均返回 200。
- OVH 精简 Caddy 时发现单文件绑定挂载仍指向旧文件节点，最终只重建 Caddy 服务；Compose 同时重建了 OVH Frontdoor，Frontdoor 在 Caddy 启动前恢复健康。最终 OVH Frontdoor/Caddy、hd0526 Frontdoor/Background/Caddy 均健康。

## 2026-08-04 Niffler 受管理提示词按用户分组配置

## 2026-08-04 Niffler 受管理提示词按用户分组配置

- 用户澄清目标：管理员创建 CTF/渗透和成人用户分组并分别配置提示词，用户通过切换 API Key 的用户分组决定使用哪套配置。
- 已确认当前线上实现错误地以调度分组为配置来源；调度分组请求头和绑定机制不应参与提示词配置选择。
- 新方向固定为服务端可信链路：API Key 鉴权结果 → API Key 当前用户分组 → 用户分组受管理提示词配置；不接受客户端请求头决定配置。
- 用户端继续使用现有 API Key 创建/编辑页面选择分组；管理端配置入口将从“调度策略”迁移到“用户分组”。
- 当前开始核对用户分组存储结构、API Key 鉴权上下文、管理端保存入口和数据迁移范围；尚未修改业务代码、提交或部署。
- 已定位用户组前后端保存结构和鉴权联表查询；计划新增 `user_groups.managed_instructions` 并随鉴权快照一次读取。
- 已完成 PostgreSQL、MySQL、SQLite 字段迁移、仓库映射、鉴权快照和导入导出接入。
- 请求注入已改为读取 API Key 所属用户分组；三种格式、请求内幂等、用户分组配置冲突和切换调度分组不改变配置的测试通过。
- 管理端注册表接口已迁到 `/api/admin/user-groups/managed-instruction-profiles`，调度分组旧接口和保存校验已删除。
- 控制台配置区已迁入用户分组编辑表单；用户端继续复用 API Key 的现有分组选择，不增加第二套选择器。
- 用户操作测试已覆盖 API Key 从默认分组切换到成人分组；运行时隔离测试确认安全分组与成人分组分别注入各自规则，不会互相混入。
- 数据层 508 项、调度核心 9 项、前端 563 项测试全部通过；网关 2969 项中 2968 项通过，唯一既有视频轮询时序测试单独重跑通过。
- Rust 全目标 Clippy、格式检查、数据库结构生成检查、前端类型检查、目标 ESLint 和生产构建均通过。
- 用户分组配置区的静态与运行时检查得到 `PASS ui-review gate`；临时审查入口和服务已清理。普通设置表单没有每帧布局测量、模糊层或高成本动画。
- 本轮未提交、推送或部署，也没有修改生产用户组、API Key 或数据库数据。

## 2026-08-04 Niffler 受管理提示词审查修复

- 用户确认修复最终审查发现的三个问题；当前先更新需求与架构说明，再修改代码。
- 修复范围固定为：重复执行最终处理时不再次运行路由正文修改；请求快照比较实际受管理提示词配置；控制台明确区分未配置和已关闭。
- UI 方向保持现有 Web 管理设置表单和紧凑组件，只增加真实的“未配置”状态，不重做页面结构。
- 三条失败测试已加入：旧实现会在第二次最终处理时覆盖已注入正文，也会把未配置分组显示成默认安全配置；同版本配置变化测试正在修正请求样例后复测。
- 后端已经修复重复执行和同版本配置变化：统一后处理识别已注入状态后不再重复运行 Provider Request 路由修改，请求快照同时比较 `managed_instructions` 实际配置值；4 项最终策略测试通过。
- 控制台未配置分组现在保持空选择、不显示版本摘要，并显示“未配置”；首次启用仍会明确写入默认配置。注册表重试测试已同步新的未配置语义。
- 后端受管理提示词相关 17 项测试、最终策略 4 项测试和网关全目标 Clippy 均通过。
- 前端相关 14 项与全量 565 项测试、类型检查、目标 ESLint 和生产构建均通过；构建只报告项目原有的浏览器数据过旧和大分块提示。
- 配置区静态与运行时审查得到 `PASS ui-review gate`，临时审查页面已删除；本次没有增加每帧测量、重排、模糊层或高成本动画。
- 本轮未提交、推送或部署，也没有修改生产配置。

## 2026-08-03 Niffler 受管理提示词配置

- 分组改造已完成：`routing_groups.config_json.managed_instructions` 现在是唯一配置来源，全局模型编辑页、旧注册表路径、模型保存校验和运行时模型数据库读取均已删除。
- 请求级共享快照固定路由分组 ID、版本和解析后的配置；同组内更换模型、Provider、Endpoint 或账号继续使用同一配置，切换到不同分组或版本会明确失败。
- 路由分组创建、修改和发布统一校验受管理提示词；新注册表接口归入路由管理权限，即使没有路由数据读取器也能返回两个公开配置的版本和摘要。
- 控制台配置区已迁入调度策略页，位于分组基本信息和排序设置之间；统一排序和按模型排序共用整个分组的配置，关闭时仍可修复已删除的配置 ID。
- 后端相关 24 项、前端组件与配置工具 15 项、路由核心 9 项、前端全量 564 项、类型检查、目标 ESLint、生产构建、Rust 格式和网关编译检查均通过。
- 网关最终全量回归为 2967/2968；唯一失败是既有视频后台轮询测试在全量负载下超过自身 500 毫秒等待上限，同一测试单独重跑通过。
- 新配置区使用真实 Vue 组件完成静态与运行时检查，结果为 `PASS ui-review gate`；临时入口和 Vite 服务已清理。普通设置表单没有每帧测量、重排、模糊层或高频动效风险。
- 前端全仓库 ESLint 仍有其他既有页面的 130 个错误；本次修改的 13 个前端文件已单独以零警告通过，不在本轮扩大修改范围。
- 未提交、推送或部署，也没有修改生产数据库和线上分组配置。
- 用户要求重新设计为按路由分组配置，不再按单个全局模型配置；已启动分组配置阶段，先核对分组存储、请求选择和控制台入口。
- 已确认分组配置本身就是版本化 JSON，无需数据库迁移；请求上下文已经携带选中分组的完整配置，可删除每次请求按全局模型读取配置的开销。
- 已确认控制台应在分组基本信息与排序设置之间增加受管理提示词区域，配置作用于整个分组，不跟随“统一排序/按模型排序”切换。
- 已完成 UI 工作流：平台为 Web，修改现有路由分组编辑页，继续使用现有后台组件、设计令牌和紧凑布局；最终复审固定使用 `compact-popup` 检查档位。
- 已定位后端统一校验入口 `validate_config_json`，可覆盖分组创建、修改和发布；已确认前端规范化函数必须保留新字段，否则保存其他分组设置会误删配置。
- 已确认分组选择发生在请求正文转换前，选中的分组 ID、版本和配置会进入共享请求输入；运行时快照将改为核对分组 ID 与版本，并允许同组内正常更换模型、提供商或账号。
- 已先更新外部需求文档和 Aether 架构记录，固定分组配置位置、选择语义、快照冲突、运行记录字段、管理接口、控制台位置、测试要求和不迁移未发布旧配置的边界。
- 已定位旧注册表接口、模型保存校验、七个最终请求调用点及其架构测试，准备分批迁移并删除模型侧入口。
- 已将运行时配置来源改为请求中的路由分组上下文，快照记录分组 ID 与版本；七个最终请求入口不再传入全局模型 ID，也不再读取全局模型数据库。
- 已更新三格式最终策略测试草案：同一分组依次覆盖 Responses、Chat 和 Claude，并准备通过共享快照验证不同分组切换会被拒绝。
- 用户要求修复审查发现的三个问题；本轮按文档先行处理配置 ID 规范、关闭状态的未知配置修复操作，以及三种最终格式的真实最终策略测试。
- 已同步外部需求文档和 Aether 架构记录：配置 ID 必须精确匹配，关闭状态仍能修复无效配置，最终策略测试必须读取数据库中的全局模型配置并覆盖三种格式。
- 后端不再只在运行时删除 `profile_id` 首尾空格；保存和运行统一拒绝带首尾空格的 ID。
- 控制台在功能关闭时仍显示已经删除的配置错误，并允许直接选择现有配置完成修复，选择过程中不会启用提示词注入。
- 最终策略测试现在使用真实全局模型仓库配置，依次验证 Responses、Chat 和 Claude 请求体、跨格式处理、运行记录位置，以及同一请求切换全局模型时拒绝继续。
- 管理端创建接口回归测试确认：即使功能关闭，带首尾空格的配置 ID 仍返回 400，且不会写入全局模型仓库。
- 本轮相关后端测试全部通过：16 项受管理提示词与架构测试、1 项三格式最终策略测试、1 项管理端保存测试；前端 7 项组件和载荷测试通过。
- 前端类型检查、目标 ESLint、生产构建、Rust 全工作区格式检查、网关全目标 Clippy 和差异空白检查均通过。生产构建只有既有的浏览器数据过旧和大分块提示。
- 使用真实 Vue 组件和本地模拟注册表完成关闭状态旧配置页面检查，结果为 `PASS ui-review gate`；临时入口与两项本地服务已删除并确认停止。
- 界面改动只改变选择框的可操作条件和错误显示条件，没有动画、循环布局测量、模糊层或其他高频交互性能风险。
- 已按用户确认的范围完成收敛：安全研究与逆向工程合并为 `security_research_v1`，保留 `adult_fiction_v1`；控制台不再提供通用、化学和武器配置，`core_v1` 只作为两个配置共享的内部正文。
- 已先同步外部需求文档和 Aether 架构记录，再修改提示词文件、后端注册表、管理接口断言、控制台默认选择和配置载荷测试。
- 离线正文契约测试现在逐项验证安全、CTF、逆向、补丁和 Keygen 规则都存在于 `security_research_v1`，成人规则只存在于 `adult_fiction_v1`，两类专业规则不会互相混入。
- 最终回归已完成：`aether-ai-formats` 381 项、`aether-gateway` 2964 项库测试及 28 项附加测试全部通过；60 项架构、14 项注入、9 项请求快照、19 项全局模型管理和 33 项管理路由测试也已单独通过。
- Rust 全工作区格式检查、差异空白检查、`aether-ai-formats` 与 `aether-gateway` 全目标 Clippy（警告按错误处理）均通过；本次新增行和未跟踪文件的敏感模式扫描无匹配。
- 前端类型检查、目标 ESLint、6 项组件与载荷测试和生产构建通过；构建仅报告既有浏览器数据过旧和大分块提示，没有编译错误。
- 全量测试发现并修正最小运行环境兼容问题：没有全局模型读取能力时固定为空配置并保持默认关闭；读取能力存在时，记录缺失、读取失败和配置无效仍明确失败。
- 当前保留共享核心及两份版本化专业提示词源码；正文规范化、唯一组合、SHA-256、配置解析和未知配置错误均集中在一个模块。
- 已在全局模型创建和更新记录构造前校验 `config.managed_instructions`，无效配置不会进入数据库。
- 注册表与注入模块 14 项单元测试通过，覆盖两个配置、专业正文隔离、正文规范化、固定摘要、旧配置拒绝和无效配置。
- 已增加请求级全局模型配置快照，并将统一最终处理接入 Responses、Chat、标准格式、同格式透传、文件、图片和视频决策；网关增量编译检查通过。
- 已实现 Responses、Chat 和 Claude 字段适配、`if_missing`、运行元数据、可信去重、冲突错误、禁用与不支持格式跳过，并补充对应单元测试。
- 三种协议与幂等相关的 11 项网关单元测试全部通过。
- 已新增管理注册表接口、固定路径路由和本地接口测试，界面可以读取真实版本与摘要，不需要硬编码注册表。
- 已在全局模型编辑表单增加紧凑的受管理提示词配置区，提供启用、配置、加入方式、版本、完整摘要和组合顺序，并覆盖加载、关闭、接口错误与重试状态。
- 已补充前端类型、注册表 API、中英文文案、配置载荷测试和配置区交互测试。
- 前端类型检查通过，配置区和载荷 6 项测试全部通过；当前进入后端与契约测试补充。
- 已固定两个配置的预期 SHA-256，补充图片后缀逐字节保留、三协议 `if_missing`、摘要冲突、Provider Request 路由结果保留、请求输入克隆共享快照和 7 条最终入口架构测试。
- 全局模型管理接口测试现在同时验证有效配置可保存、未知配置返回 400 且不会写入数据库。
- 已用真实 Vue 组件和模拟注册表完成紧凑弹窗运行时审查，结果为 `PASS ui-review gate`；临时审查入口和临时服务均已清理。
- 已修正 Provider Request 路由可能改变图片规则末尾位置的边界，并补充三项 Responses 测试；受管理提示词模块当前 14 项测试全部通过。
- 已完成代码差异核对，并新增 `docs/architecture/2026-08-03-managed-instruction-profiles.md`，固定配置、摘要、内部状态、三种协议、错误与验证语义。
- 当前进入版本化提示词注册表与配置解析实现；尚未修改请求运行逻辑和管理界面。
- 用户已确认进入 Aether 实现。
- 已启用文件化计划和 UI 规范流程，新增当前任务阶段、安全边界和关键问题。
- 已确认当前工作区存在未提交的 Codex Responses 修复；未覆盖、回退、暂存、提交或部署这些改动。
- 下一步核对全局模型配置、请求转换、`body_rules`、重试、请求记录和全局模型编辑页，再先新增 Aether 设计记录。
- 已运行 UI 工作流：平台为 Web，界面类型为全局模型编辑表单，主任务和视觉方向已固定；实现时按现有后台紧凑表单处理，最终必须通过 `PASS ui-review gate`。
- 首次 UI 工作流误填 React；代码检索确认项目使用 Vue，已停止使用该技术栈结果，准备按 Vue/TypeScript 重跑。
- 已按 Vue/TypeScript 重新运行 UI 工作流；界面结构和反馈要求保持不变。
- 初步确认全局模型现有 `config` JSON 可以承载本功能，无需数据库迁移；正在定位执行计划和请求记录的精确写入位置。
- 已确认执行计划和调度候选不携带完整全局模型配置，下一步检查请求规划阶段能否按候选中的全局模型 ID 读取配置，并确定内部状态进入现有报告上下文的位置。
- 已找到可行入口：标准候选载荷构建阶段可按全局模型 ID 读取配置；现正核对统一请求正文构建函数、Claude 分支和后续路由组修改阶段，随后写 Aether 设计记录。
- 已确认同格式旁路和路由组后置修改，正在比较两种实现：候选携带受管理配置后统一做最终处理，或各请求构建分支分别读取配置；优先选择能覆盖所有路径且不复制三套逻辑的方案。
- 已排除扩展底层候选行结构的方案，因为会影响大量无关测试构造；继续寻找请求级配置读取位置和统一最终处理入口。
- 已确认图片桥接正文有稳定后缀，可在统一最终处理时保持其位于末尾；正在检查内部候选对象能否在一次解析后携带全局模型配置，从而避免每次账号重试查询数据库。
- 已确定请求级读取方向：候选预选完成后按首个候选的全局模型 ID 读取一次配置，保存到共享决策输入；正在核对三个文本规划入口是否都能使用同一辅助函数。

---

## 2026-08-03 全部本地改动提交与生产同步

- 用户已明确要求提交全部本地改动，并保持本地、远端和生产版本一致。
- 已进入发布前范围、安全和远端基准核对；尚未暂存、提交、推送或部署。
- 已确认仓库公开、本地 `main` 基准过旧且与远端提交图异常分叉；发布提交必须建立在远端最新 `main` 上。
- 正在检查 `outputs/` 和其他新文件是否包含真实用户数据或凭证。
- 已确认 `outputs/` 是真实用户计费报告，禁止进入公开仓库；新增忽略规则并保留本地文件。
- 继续检查脚本、环境示例和其余差异是否含凭证。
- 已跟踪差异的敏感模式扫描没有发现匹配；新文件首次扫描因 zsh 没有拆分多行变量而未执行，已改用 NUL 分隔方式继续检查。
- 新文件敏感模式复查通过，环境示例均未包含凭证，其余新增文件大小正常；发布范围安全检查完成。
- 正在以远端最新 `main` 创建隔离发布工作区，避免旧本地分支混入已经上线或无关的历史提交。
- 远端最新 `main` 隔离发布仓库创建成功，下一步机械复制全部非敏感本地改动并检查最终差异。
- 第一次隔离整理及时发现整文件复制会回退远端新内容，未暂存或提交；已切换为三方合并方案。
- 第二个隔离仓库正在下载完整对象；一次路径变量未保存导致检查落回原工作区，已确认原工作区未被修改，并改用绝对临时路径继续。
- 挂起的第二次网络克隆已停止；已从现有完整仓库创建干净三方合并工作树，未重置或覆盖原工作区。
- 三方差异应用完成，47 个文件自动合并，4 个文件有重叠冲突；正在逐段合并并保留两边有效逻辑。
- 4 个冲突文件已逐段合并且无残留标记；Rust 格式、Git 差异和 Shell 语法检查通过。
- 正在运行后端完整测试和相关数据、计费、前端及运维脚本测试。
- 网关全量测试首次结果为 2940/2942，通过项包括新增流式换号和现有网关回归；2 项 429 暂停理由测试失败。
- 已修正账号池分类优先级：429 继续使用账号级限流暂停，流式输出前仍允许换账号；正在执行定向和全量复测。
- 网关完整复测通过：2942 项全部成功；数据层 508 项、计费 38 项测试及 Rust 全工作区格式检查通过。
- 前端相关 9 项测试、类型检查、目标代码规范检查和生产构建全部通过。
- 监控设置和生产监控脚本在 `hd0526` 隔离临时目录中使用假依赖完成测试，两项均通过；测试目录已清理，未读取或修改生产配置。
- 发布前功能验证完成，正在同步任务记录、复核最终差异并创建提交。
- 提交 `fc6b56e1` 已推送并创建 `test` 合并请求 #25；首次 CI 发现数据层一处 Clippy 代码清晰度错误，正在做无行为变化的最小修复。
- 数据层 Clippy 修复已通过相同本地检查并推送为 `635b60df`；第二轮 CI 又发现计费测试模块位置不符合工作区规则，正在只移动测试模块到文件末尾。
- 计费测试模块已移到文件末尾；本地计费包 Clippy、38 项测试和格式检查通过。整工作区本地检查因隔离编译目录耗尽磁盘空间中止，清理的只有 15.3 GiB 可重建产物，Linux 全工作区检查继续交由 CI 完成。

## 2026-08-03 远端与生产版本核对

- 已通过 GitHub API 获取远端全部现有分支并按文件内容校验；确认 22 个 Provider 统计文件已经位于远端 `main`、`test` 和对应修复分支。
- 正在只读核对生产部署提交号、镜像和数据库迁移状态。
- 首次检索发布脚本因不存在文件的 shell 通配符被 zsh 拒绝，未执行读取；已改用明确文件列表继续核对。
- 已确认生产状态文件、两个应用镜像和镜像 revision 全部为远端 `main` 最新提交 `fac7862d`；两个应用容器均健康。
- 正在逐项比较 4 个 Provider 后续文件、5 个 CCSwitch 文件和 3 个 GPT-5 价格文件。
- 文件级比较完成并更正分类：Provider 剩余 3 个差异均为测试、文档或等价改写；CCSwitch 5 个后续口径改动尚未进入生产提交；GPT-5 价格调整实际涉及 4 个文件。
- 下一步查询生产数据库迁移记录、模型启用状态和实际价格，确认 GPT-5 是否曾通过数据库方式上线。
- 生产迁移记录确认 Provider 统计迁移已成功执行，GPT-5 价格迁移不存在；继续检查生产模型价格，排除人工修改数据库的可能。
- 生产实际价格、号池覆盖价格、禁用模型状态和价格影子表均与本地 GPT-5 迁移目标一致；确认价格已通过数据库操作上线，但对应代码和迁移没有提交。
- GitHub 提交历史确认 CCSwitch 基础兼容早已上线，本地 5 个文件属于尚未上线的后续展示口径修改。
- 核对完成：远端、生产镜像、迁移历史、生产模型价格和提交历史已相互印证；未修改生产服务、数据库或配置。

## 2026-08-02 OpenAI Responses 输出前自动换号

- 已完成线上日志、账号池状态、当前生产实现和 Codex 配置的只读排查，确认根因是 HTTP 200 SSE 内的上游容量失败与网关过早提交响应共同造成。
- 已确认现有配置没有 `stream_failover`，计划新增端点级配置而非用户本地或全局配置。
- 已确定界面范围：沿用现有 Web 管理后台的端点编辑表单，只在开关启用后显示必要参数，补齐保存、禁用和校验反馈。
- 已完成 `planning-with-files` 与 `using-ui-polish` 的执行前检查，平台确定为 Web 管理后台、界面类型为紧凑端点设置表单、主任务为配置输出前自动换号、视觉方向为沿用现有后台。
- 已确认本地分支落后于包含有限预读修复的 `origin/main`，后续只修改本任务文件并保留全部既有未提交改动。
- 已新增架构记录 `docs/architecture/openai-responses-pre-output-failover.md`，写明目标、非目标、配置语义、提交边界、模型级暂停、监控和验证方式。
- 已完成现有实现边界核对，进入端点配置契约和运行时解析阶段；尚未修改生产配置或生产服务。
- Phase 2 完成：端点配置新增 `stream_failover` 契约、默认关闭、运行时防御性限值和管理接口写入校验。
- 端点运行计划会保存解析后的配置，后续流处理无需再次读取数据库。
- 验证：`cargo test -p aether-gateway orchestration::policy --lib`，6 项通过，0 项失败。
- 当前进入 Phase 3：实现 SSE 事件边界、输出前换号和账号模型级暂停。
- Phase 3 完成：完整 SSE 事件决定提交边界，生命周期事件继续等待，失败事件可在输出前换号，其他事件立即提交并锁定当前账号。
- 新预读使用端点级总等待时间和暂存上限；关闭配置时继续使用原请求逻辑。
- 容量失败会暂停“提供商账号 + 实际上游模型”组合，其他模型仍可使用该账号；设置、跳过和超限提交均有结构化日志。
- 验证：事件解析 5 项、容量失败跨 8 个生命周期帧换号 1 项、失败分类 9 项、模型暂停键 1 项、调度跳过 1 项，全部通过。
- 当前进入 Phase 4：增加端点编辑界面和前端校验。
- Phase 4 完成：`openai:responses` 端点新增“实际输出前自动换号”配置区，关闭时隐藏参数，开启后可编辑失败后换号次数、最长等待、暂存上限和模型暂停时间。
- 界面沿用现有端点卡片、输入框、开关、保存和撤销反馈；配置保存时保留端点其他 JSON 设置。
- 前端设置辅助函数 5 项测试和 `vue-tsc --noEmit` 类型检查通过。
- 当前进入 Phase 5：补齐异常路径和整体回归验证。
- Phase 5 完成：后端覆盖默认关闭、配置限值、完整 SSE 事件边界、跨 8 个生命周期帧且延迟 800 毫秒的容量失败换号、参数错误不换号、实际输出后不换号，以及账号模型级暂停和调度跳过；前端覆盖默认值、单位转换、校验、变更检测和保留其他配置。
- Phase 6 完成：`cargo test -p aether-gateway --lib` 全部 2922 项通过；前端目标 5 项测试、类型检查和目标 ESLint 通过；Rust 目标文件格式检查、全工作区 `git diff --check` 通过。
- 新配置组件通过 `ui-review` 静态和运行时检查；界面使用现有设计令牌，无高频指针或滚动路径，也没有新增模糊、滤镜、阴影或动画性能负担。
- 首次整库测试发现 3 项问题后已修正：保留旧通用失败分类语义、将 OpenAI 格式判断改走项目规定入口；三项定向回归和整库复测均通过。
- 最终实现与架构记录一致：配置默认关闭，只作用于 `openai:responses` 端点；容量错误使用账号模型级暂停，限流继续使用现有账号级规则；暂存上限在完整执行帧边界检查。
- 本轮只修改本地代码、测试和文档；未部署、未重启服务、未修改生产数据库或生产端点配置。

---

## 2026-08-02 Provider 账号计费统计一致性修复

- 已新增请求级 Provider 用量贡献和修订号，usage 写入、结算完成、重复结算、超时恢复及失败
  恢复统一同步贡献。
- 已移除迁移时的 usage 全表锁和一次性历史扫描，改为后台按账号分批整理，逐账号重建累计统计
  和 Codex 窗口统计。
- 已将窗口缺失检查改为比较请求数、Token 和费用，已有错误零记录也会进入修复队列。
- 已补充统计总锁和账号锁，账号重建会吸收未处理的旧统计事件，避免并发重复或覆盖。
- 已实现修复失败记录、延迟重试和后台告警，并补充批量边界、并发、零费用和 8 位金额精度测试。
- 验证通过：数据层与网关测试目标编译、直接相关测试、Schema 检查、格式检查和差异检查。
  PostgreSQL 集成测试因本机共享内存不足被跳过，尚未在本机真实执行。
- 未修改生产数据库，未部署生产。

## 2026-08-01 提供商测试、单账号测试与账号并发

- 已完成账号级实时并发聚合：从 Redis 请求令牌按提供商账号统计，不写数据库；账号列表返回 `current_concurrency`，并与 `concurrent_limit` 一起显示。
- 已完成单账号测试入口：账号行按钮复用现有模型测试弹窗，前端发送 `api_key_id`，后端再次校验账号属于当前提供商且支持所选协议，并以 `direct` 模式固定请求账号。
- 已完成协议模板优先测试：支持模型选择、协议端点、测试提示词、流式开关、图片尺寸/数量/格式；请求头和请求体 JSON 保留为默认收起的高级编辑。
- 已补充 OpenAI Responses、Responses Compact、Gemini `generateContent` 和 OpenAI 图片/Responses 图片生成模板；继续复用现有请求追踪、错误详情和图片预览。
- 已修正静默并发刷新失败时保留旧数据、分页递归透传静默参数、组件卸载清理定时器，以及号池配置存在时避免重复读取并发。
- 验证通过：`npm run test:run -- ...`（3 个目标文件，59 项测试）、`npm run type-check`、`cargo fmt --all -- --check`、`cargo test -p aether-gateway provider_pool_demand`、账号列表接口测试和 `git diff --check`。
- 前端首次测试因本地缺少 `vue-i18n` 未进入用例，按锁文件执行 `npm ci --ignore-scripts` 后恢复；npm 报告依赖树存在既有安全告警，未执行自动升级。

## 2026-07-28 GitHub 测试环境与晋级流水线配置

- 用户确认 `ryfineZ` 是本人，并要求立即完成管理员配置和测试部署链路。
- 已核对 PR #14：最新 CI 全部通过，工作流检查名称和测试域名正确。
- 已记录并隔离当前工作区既有未提交改动，开始核对 GitHub 管理权限和现有保护配置。
- GitHub 管理权限、production Environment、main Ruleset 和 `g-dxw` 协作者权限均已核对；进入测试服务器专用部署账号配置。
- 已生成独立 `niffler-test-deploy` Ed25519 密钥，私钥仅以 `0600` 保存到指定 `vps_nodes` 目录，并已写入 GitHub `test` Environment Secret。
- 已创建 test Environment、部署分支规则和 test Ruleset；已将 `g-dxw` 加入 production reviewer。
- 2026-08-02 root 密码认证恢复可用后，已创建并保存 `niffler-test-admin` 管理密钥，写入 root；已将既有 `niffler-test-deploy` 密钥写入受限部署账号。两把密钥均完成真实登录验证，服务器原有密钥保留。
- PR #14 已获得 `ryfineZ` 审批，所有必需检查也已通过；实测 `main` 已是 PR head 的祖先，
  GitHub 仍返回 `BLOCKED`，不是分支未同步。
- 复核发现原 PR 的测试部署无法可靠完成首次部署：部署账号文档与 GitHub 配置不一致，源站
  健康端口被写死且未配置，空测试环境没有 `app` 容器时会停止。已在隔离分支补齐部署账号、
  显式健康地址和首次测试部署逻辑，直接相关脚本测试通过。
- 独立审查又发现首次迁移检查可能误用 `.env` 旧镜像、测试配置误指向生产目录缺少硬拦截；
  两项均已修复并完成复审。当前补丁等待提交到可更新的 PR 分支。
- 已通过本机 GitHub SSH 身份将补丁推送到 PR #14；当前测试域名 HTTPS 与健康接口均已从
  公网验证。公网无法确认 Nginx 的本地 upstream，不能据此改为 8084；已恢复原有 18084
  约定，并记录为等待 Nginx upstream 核对的配置。
- PR #14 的最新 CI 已全部通过（含 Gateway 测试和 PostgreSQL 冒烟测试），已重新审批并通过 GitHub 页面以普通 Merge 合并到 `main`；CLI 授权缺少 workflow scope，因此没有用于合并。
- 合并后的 `main` Release Tooling CI 与 Frontend CI 均已通过，未触发生产部署。
- 按团队流程调整 `test` Ruleset：Required approvals 已由 1 改为 0；`main` 仍要求 1 人审批，其他保护规则未变。
- 已备份并修正测试服务器 SSH 生效配置，关闭全局密码认证并限制为公钥认证；`sshd -t`、服务热加载、root 管理密钥、受限部署密钥和密码认证拒绝均已验证。测试容器保持 healthy，未修改或重启生产服务。

---

## 2026-07-28 生产服务器 Telegram 监控

- 用户同意部署磁盘、数据库、Redis、应用容器和网站健康 Telegram 监控。
- 已完成两台服务器只读基线检查；`hd0526` 磁盘使用率 83%，其余检查正常。
- 已新增生产监控运维文档，下一步实现去重、连续失败判断和恢复通知。
- 已实现通用监控脚本、systemd service、timer 和两台服务器配置模板。
- Bash 语法、ShellCheck、systemd unit 和差异检查均通过。
- 两台服务器的 Telegram 测试摘要已投递成功，timer 已启用并处于 active。
- `hd0526` 的 83%磁盘预警已发送；第二次及后续相同状态检查没有重复通知。
- 隔离状态机测试通过：连续第 3 次异常告警一次，持续异常不重复，恢复通知一次；
  磁盘预警、严重和恢复也按状态变化通知。
- 最终生产复查通过：监控任务结果 success，核心容器健康，公开健康接口 HTTP 200。

## 2026-07-28 Telegram 数据库备份失败通知

- 已验证 Bot Token 对应 `niffler_ops_alert_bot`。
- 本机访问 Telegram API 被重置，改由 `rn01` 调用后成功。
- 用户发送开始消息后，已取得私人 Chat ID；正在新增失败通知脚本和 systemd 服务。
- 已新增 Telegram 通知脚本和 systemd 服务，并给备份服务配置 `OnFailure`。
- 脚本通过 Bash 语法检查、ShellCheck，systemd unit 通过预检查。
- 已向私人聊天投递明确标注的测试消息，Telegram API 返回成功。
- 凭据权限、失败触发关系和原备份定时器状态检查通过。
- 用户要求成功备份也发送通知；已将成功消息接入备份服务的 `ExecStartPost`。
- 已补发本次凌晨备份的成功消息，Telegram API 返回投递成功。

## 2026-07-28 生产网络与数据库连接加固

- 已完成只读核查：确认公网 `8084`、数据库防火墙、PostgreSQL 角色、TLS 状态、
  应用连接变量和现有通知条件。
- 已确认 `rn01` 的 5432/6379 已按来源地址限制，当前未对其他公网来源开放。
- 已确认应用支持强制 PostgreSQL TLS，但当前仍使用未加密连接和超级用户。
- 已新增生产网络与数据库加固运维记录；下一步先收紧 `hd0526:8084`。
- 已将 frontdoor 端口改为只监听 `127.0.0.1:8084`；公网连接已拒绝，本机健康
  检查、Caddy 域名入口和延迟健康复查均通过。
- 首次对象所有权转移因表自带序列不能先单独修改而在事务内回滚；业务数据和对象
  所有者未改变，正在调整执行顺序后重试。
- 调整顺序后所有权转移成功；`niffler_app` 接管数据库、120 张表和 2 个序列，
  账号无超级用户权限，迁移权限测试通过。
- frontdoor 与 background 已强制重建并切换到 `niffler_app`，两个容器均健康。
- PostgreSQL TLS 文件在重启前检查中发现一次安装和目录权限问题；当时数据库没有
  重启。修正后使用 PostgreSQL UID 的隔离容器确认三项文件可读。
- PostgreSQL 已启用 TLS 和远程 `hostssl` 规则，明文连接测试被拒绝，TLS 测试
  使用 TLSv1.3。
- 应用已设置强制 TLS 并重新创建；最终 15 条远程会话全部为
  `niffler_app + TLSv1.3`。
- 最终检查通过：公网 8084 关闭，主页、认证设置和公开健康接口为 HTTP 200，
  核心容器健康，自动备份定时器 active，备份结构检查成功。
- 外部失败通知因没有 SMTP、Webhook 或消息接收地址而未接入，现有状态文件和
  systemd 日志继续生效。

## 2026-07-28 rn01 首次数据库备份与恢复验证

- 用户已明确同意执行首次备份、上传和恢复验证。
- 已完成生产只读检查：`rn01` 可用 53 GB，数据库约 16 GB，PostgreSQL 15.18
  健康且没有长事务。
- 已确认本机有 62 GB可用空间和可用 Docker，可在本机完成隔离恢复，不占用生产盘。
- 首次最大关系查询因远程 shell 转义失败；改用 Base64 传递 SQL 后成功，数据库
  没有发生修改。
- 正在先补充备份与恢复运维文档，然后执行完整导出。
- 运维文档已新增；首次导出因误带 `pg_dump` 不支持的 `-X` 参数，在读取数据前
  停止，只留下0字节临时文件，数据库和业务未受影响。
- 移除错误参数后完整导出成功；16 GB数据库压缩为约1.36 GB，结构检查和SHA-256
  计算通过，导出期间数据库持续健康。
- 本机从 `rn01` 复制速度过慢，已停止该复制并删除5.7 MB临时片段；改为让
  `rn01` 使用最小权限凭据直接上传R2。
- R2备份和校验文件上传成功；HEAD检查确认对象大小和SHA-256元数据正确。
- 已从R2重新下载备份到本机，大小与SHA-256均一致；进入隔离恢复验证。
- 已使用 PostgreSQL 15.18 隔离容器完整恢复 R2 下载文件；120 张 public 表和关键
  业务表均可读取，无无效索引或未验证约束。
- 已删除隔离恢复容器、临时卷、本机下载文件及 `rn01` 上的首次临时备份；R2 中
  保留经过恢复验证的备份。
- 已安装 `rclone`、自动备份脚本及 systemd 定时器；每天北京时间 04:30 执行，
  并配置每日、每周、每月保留期限。
- 已手动启动 systemd 服务完成真实检查；第二份备份上传、大小校验、SHA-256
  校验和本地临时文件清理均成功，生产 PostgreSQL 全程正常。
- 最终检查通过：定时器已启用且处于 active，服务结果为 success，凭据权限为
  `0600`，R2 两份备份对象均存在。
- 当前失败信息会写入状态文件和 systemd 日志，但尚无外部通知渠道；该项作为
  后续监控工作保留。

## 2026-07-27 数据库磁盘事故止血与恢复

- 用户要求处理事故并给出建议。
- 已确定先执行可逆止血：切换 `request_record_level=basic`；历史正文删除等待用户
  明确确认。
- 首次更新时 PostgreSQL 已再次进入恢复模式，事务被拒绝且未修改配置；正在重新
  检查数据库和磁盘状态。
- 已确认 rn01 根分区再次为 100%，PostgreSQL 因无空间写恢复检查点而循环重启。
- 已删除四个没有容器引用且可重新下载的镜像，释放约 1.3 GiB；未动业务容器、
  数据卷和 Niffler 回滚镜像。
- PostgreSQL 已恢复健康，生产 `request_record_level` 已从 `full` 改为 `basic`。
- 两轮线上复核通过：认证设置与 OAuth Provider 接口均在约 0.1 秒返回 200；
  切换后没有新增 `usage_body_blobs` 记录，磁盘未继续快速增长。
- 可逆止血已完成；下一步需要用户确认是否删除全部历史请求/响应正文，以立即释放
  约 52 GiB 数据库物理空间。
- 已只读核对正文关联：删除 `usage_body_blobs` 不会级联删除 `usage`、计费、用户
  或密钥记录；需要同时清理审计表中的正文引用和失败状态。
- 用户已明确确认删除历史正文；已清空 345,380 条 PostgreSQL 正文和 55,352 条
  无对象存储失败元数据，根分区释放至约 54 GiB 可用。
- 已清除 657,302 条历史审计记录中的正文引用和状态，并以低共享内存参数完成
  `VACUUM ANALYZE`。
- 最终验证通过：正文表、对象元数据表和正文引用均为 0；PostgreSQL 健康，首页、
  认证设置和 OAuth Provider 接口均为 HTTP 200、约 0.07–0.08 秒；用户、密钥、
  用量和计费数据未减少。
- 最终日志发现 Redis 用量队列存在 860 条长期重试的历史事件，引用已删除的
  Provider 密钥；已记录为后续代码与数据修复项，本次未直接删除未核对的计费事件。

## 2026-07-27 线上登录卡顿诊断

- 已启动只读性能诊断；尚未修改代码、生产配置、数据库或容器状态。
- 正在建立外部请求耗时基线，并定位登录页面实际调用的接口。
- 外部基线已发现认证配置接口稳定等待约 5.6 秒并返回 501；主页和健康接口仅约
  0.6 秒，诊断范围已收敛到认证公开接口及其依赖。
- 已通过响应头确认异常请求进入 `public_proxy_passthrough`，Rust frontdoor 没有本地
  实现，正在检查转交目标和生产日志。
- 已确认登录框初始化会整体等待注册设置、认证设置和 OAuth Provider 三个请求，
  当前故障接口直接位于用户登录前的关键路径。
- 代码复核发现设置接口已经存在，但数据库或配置读取错误会被转成“未实现”501；
  正在用其它依赖数据库的公开接口和登录探针交叉验证。
- Phase 1、2 完成：真实登录探针、OAuth Provider 和站点公开接口均证明 PostgreSQL
  连接池取不到连接；已进入生产主机与数据库只读检查。
- 应用主机资源正常；日志已找到数据库端“磁盘无剩余空间”以及随后连接重置的明确
  时间线，下一步直接检查 PostgreSQL 主机磁盘和服务状态。
- 已确认 `rn01` 根分区 100% 且 Niffler PostgreSQL 因无法写锁文件持续重启；
  正在定位 75 GiB Docker 持久化卷中具体增长对象。
- 已定位到 Niffler PostgreSQL 数据卷内文件节点 `983372`，约 51.4 GiB 且在近两天
  快速增长；正在确认它对应的业务表。
- PostgreSQL 当前无法启动，准备使用 PostgreSQL 社区的只读页文件解析工具读取
  `pg_class` 副本，不在数据库主机安装软件或改动数据卷。
- 已在本机临时目录构建只读页解析工具并映射系统目录，确认 51.4 GiB 文件节点属于
  `usage_body_blobs` 的大字段数据；生产数据卷未被修改。
- 已确认生产已包含 7 月 25 日的大对象存储改动，但生产环境没有配置对象存储；
  正在核对未配置时的正文落盘和清理行为。
- 代码核对完成：无对象存储时完整正文会无大小上限地压缩写回 PostgreSQL；
  这与生产 51.4 GiB `usage_body_blobs` 快速增长完全吻合。
- 已核对正文表结构和默认保留策略；下一步确认生产实际记录级别、保留天数和清理
  工作是否在故障前正常执行。
- 已离线确认生产实际为 `full`，保留 1/2 天、批量 1000；正在核对清理任务最近运行
  情况和事故影响范围。
- 已确认连接池固定等待 5 秒，自动清理仅每天 03:00 一次；本次部署后的 background
  在 03:00 之后启动，未执行清理便于当日下午写满磁盘。
- 最终复核期间 PostgreSQL 自行完成崩溃恢复，接口暂时恢复；磁盘仍为 99%，正在
  利用只读查询确认正文分布和表膨胀程度。
- 用户说明已手动重启 `rn01`；重启后接口恢复，数据库聚合确认 52 GiB 正文表、
  400,503 条正文记录，且仍在继续写入。正在确认旧正文未清理的直接原因。
- 清理历史已确认：此前每日清理有效，但 7 月 27 日部署后错过 03:00 且未补跑，
  目前约 29.3 万条过期正文仍占用数据库盘。
- 最终复核通过：重启后 PostgreSQL 与登录关键接口已恢复；诊断完成，未修改线上
  代码、配置或数据。剩余风险是磁盘仅余约 1.7 GiB，且 `full` 正文仍在持续写入。

## 2026-07-27 GitHub 受保护生产发布与双人审核

- 用户同意继续配置 GitHub 生产发布环境和第二名审核者。
- 已启用文件化计划，创建分支 `codex/secure-production-deploy-20260727`。
- 正在执行 Phase 1，只读审计 GitHub Actions、环境、Secrets、协作者、工作流和
  生产主机权限；尚未生成密钥、修改服务器授权或写入 GitHub Secret。
- 前一轮审查误清理的 172 个本地上游版本标签已从 `upstream` 全部恢复，
  远端仓库和代码提交未受影响。
- GitHub 审计确认仓库没有环境、Secrets、Variables、Deploy Keys 或 Runner，
  只有 `ryfineZ` 一名管理员。
- 生产主机审计确认当前入口直接登录 root，唯一授权公钥无命令限制，且没有专用
  发布用户；下一步必须先确定受限账户和受控命令协议，不能直接上传 Actions 密钥。
- 已检查 `Build App Image` 和 `deploy-ci-artifact.sh`：现有协议要求 SSH 用户查询
  Docker 状态并运行任意远程脚本，不能直接降权复用。
- 已核对 GitHub 官方环境与 Actions 安全文档：生产 Secret 应放在仅允许 `main`
  的环境中，第二名维护者作为环境审核者并禁止自行批准；生产 Job 使用的 Action
  应固定到完整提交 SHA。
- 已确认生产 `.env` 和 Compose 文件仅 root 可读，专用发布用户不需要也不应加入
  Docker 组；固定部署器已经覆盖主线、镜像 revision、迁移、健康和回退验证。
- Phase 1 完成，进入 Phase 2：先更新发布设计与运维文档，再修改脚本和工作流。
- 已先更新 `docs/operations/ci-image-deploy.md`，记录 GitHub 环境保护、专用用户、
  固定 SSH 协议、root 包装器、密钥轮换和验证要求。
- Phase 2 完成，进入 Phase 3：实现并测试受限发布链路。
- 已先新增受限 SSH、root 包装器和安装脚本的安全边界测试；首次运行按预期失败，
  原因是三个实现脚本尚不存在。
- 已实现受限 SSH 命令分派器、root 固定参数包装器和服务器安装脚本；只允许查询
  状态、上传准确提交镜像和部署该提交，不向 Actions 开放交互式 shell 或 Docker。
- 首次成功路径验证发现 macOS 的 `/var` 与 `/private/var` 真实路径差异，正在修正
  包装器的目录边界比较；权限、提交号、所有者、模式和文件大小检查保持不变。
- 路径规范化修正后，受限生产访问测试已全部通过。
- 已先新增发布客户端下载、上传哈希核对、固定命令调用和禁止回滚的测试；下一步
  实现 `deploy-ci-artifact.sh` 的受限 Actions 模式。
- `deploy-ci-artifact.sh` 已支持受限 Actions 模式：沿用成功 CI 产物和提交继承
  检查，通过固定 SSH 命令查询、上传、核对 SHA-256 后部署，并禁止自动化回滚。
- 已新增独立 `Deploy Production` 工作流：仅允许 `main`，要求输入提交等于当前
  `main`，使用 `production` 环境、主机指纹校验、串行发布和完整 SHA 固定 Action。
- 已将新脚本和测试接入 `Build App Image` 与 `Release Tooling CI`；受限入口、
  发布客户端、固定部署器、提交继承和生产工作流安全测试均已通过。
- 隔离 Ubuntu 容器首次安装验证因新增脚本尚未设置可执行位而停止，容器和生产
  均未发生配置变更；正在修正文件模式后重试。
- 新脚本已设置可执行位；隔离 Ubuntu 24.04 容器安装验证通过：用户和目录权限
  正确，固定 `status` 命令可经白名单 sudo 执行，任意 `/bin/true` sudo 被拒绝。
- 生产主机只读兼容性检查通过：Ubuntu 22.04、OpenSSH 8.9、sudo 1.9.9 和安装
  所需命令齐全，SSH 严格权限检查开启，客户端环境变量注入关闭。
- 安全复核发现并修正主机密钥信任范围：新增独立验证脚本，只将指纹精确匹配的
  扫描密钥写入 `known_hosts`；双密钥和完全不匹配回归测试均通过。
- 全部新增和修改 Shell 脚本已通过 ShellCheck，生产工作流 YAML 语法检查通过。
- 已创建本地安全发布提交；首次推送遇到 GitHub 空响应，API 确认远端没有创建
  该分支，正在保留本地提交并安全重试。
- HTTPS 推送因当前 OAuth 令牌没有 `workflow` 权限被 GitHub 拒绝，随后使用已认证
  的 GitHub SSH 身份成功创建远端分支；API 已确认远端与本地提交 SHA 一致。
- PR #12 首轮 `Release tooling` 失败，日志确认 Linux 的 `stat -f` 返回文件系统
  信息而非文件属性；已修正跨平台参数顺序，准备同时在 macOS 和 Linux 重跑。
- macOS 回归通过；最小 Ubuntu 复验继续发现测试写死依赖 `shasum`，已改为与生产
  脚本相同的 `sha256sum` 优先策略。
- Ubuntu 24.04 普通用户环境中，受限生产访问和主机密钥验证测试均已通过；Linux
  与 macOS 的属性读取和摘要计算路径现已分别覆盖。
- PR #12 的全部检查通过并合并到 `main`，合并提交为 `f626fda2`。
- GitHub `production` 环境已创建：仅允许 `main`，禁止自审和管理员绕过；当前
  只有所有者一名审核者，所以环境处于无法自行放行的锁定状态。
- 生产服务器已安装专用用户和固定入口，但首次公钥登录被拒绝；GitHub Secrets
  尚未写入，临时私钥已删除。
- 已确认 `.ssh` 和 `authorized_keys` 由 root 所有且权限分别为 `0700/0600`，
  目标用户无法读取；进入修复分支，先更新权限设计再修改安装器。
- 安装器已改为 home 和 `.ssh` 使用 root `0755`、授权文件使用 root `0644`，
  专用用户仍不能修改；`uploads` 继续由专用用户所有并使用 `0700`。
- Ubuntu 24.04 真实 `sshd` 集成验证通过：专用公钥可以执行 `status`，交互命令
  被固定入口拒绝，目录、授权文件和上传目录权限均符合设计。

## 2026-07-23 Codex 上游配额窗口

- 已确认当前任务范围扩大为所有上游窗口，至少包含 5H、7D、1M。
- 已核对本地 sub2api 源码：上游 `/wham/usage` 返回窗口时长和重置字段，不能只按 `primary/secondary` 命名。
- 已确认 Aether 当前后端、前端、管理号池和用量统计仍存在 `weekly/5h` 硬编码。
- 已创建本阶段任务计划、调查记录和设计方向，下一步先更新设计文档再修改实现。
- 已完成后端通用窗口解析：保留上游 5H、7D、1M 和未知时长，标准代码为 `5h/weekly/1m`，未知窗口按真实秒数生成稳定代码。
- 已完成前端和管理号池通用展示，窗口消失时不再从旧快照补回；旧 `weekly` 仅作为历史兼容别名。
- 已完成账号级耗尽判断、内存和 PostgreSQL 窗口用量统计、手动重置逻辑；feature/model/workspace 窗口不计入普通请求统计。
- 已通过解析、网关快照、调度、内存统计、SQL 源码、手动重置、号池摘要和前端配额展示测试。
- 已通过 `cargo fmt --all -- --check`、`git diff --check`、四个 Rust 包的 `cargo check` 和前端 `vue-tsc --noEmit`。
- 已将未知窗口的用户展示改为中文天/小时/分钟，不再显示 `s`、`m` 或内部窗口代码；保留原始秒数用于准确换算。
- 已确认 7D 沿用 `weekly` 内部标识，界面继续显示 `7D`，避免同义代码导致当前周期用量丢失。
- 审查确认同步生图失败事件仍可能按 HTTP 200 返回、`error: null` 会被误判、未知窗口统计会截断秒数；进入修复和针对性验证。
- 已修复同步生图失败归一化后的成功判断，账号状态和成功副作用不再继续使用上游原始 HTTP 200。
- 已统一同步生图事件流分块逻辑，LF 和 CRLF 换行都能识别失败事件。
- 已为 PostgreSQL Codex 窗口增量统计增加 `BIGINT` 数值和分钟转秒溢出保护，异常快照不会中断整批用量处理。
- 最终验证通过：新增生图失败与 CRLF 测试、Codex 窗口 SQL 测试、相关四个 Rust 包编译、Rust 格式检查和差异空白检查。

## 2026-07-15 本地未提交改动审查与上线

- 已清点 24 个原始未提交文件：18 个已修改文件、6 个未跟踪文件，暂存区为空。
- 已读取项目现有计划、调查和进度记录，并确认旧会话恢复内容不适用于当前工作区。
- 已确认提交链为 `153046f0 -> fcc5a165 -> eb44f662`；本次最终提交必须位于 `eb44f662` 之后。
- 正在逐项审查 Codex 客户端版本管理实现、生图验收脚本、相关文档和测试。
- 初始检查通过：`aether-model-fetch` 41 项测试、网关模型获取 18 项测试、Python 验证脚本 3 项测试、Rust 格式和差异空白检查。
- 审查发现空模型目录会清空现有权限、损坏版本状态会停止自动检查两项问题；已先更新客户端版本管理设计记录。
- 已补充空目录回归测试并确认修改前失败；已实现空目录失败保护和损坏版本状态恢复逻辑。
- 修复后验证通过：网关模型获取 19 项测试、模型获取库 41 项测试、Python 验证脚本 3 项测试、严格 Clippy、Rust 格式和差异空白检查。
- 已完成全部未提交文件的代码、文档、安全信息和提交链复核，未发现遗留密钥或凭证。
- 已创建提交 `44354c0e fix(codex): 自动更新上游模型目录版本`，并接到 `eb44f662` 之后；`fcc5a165` 图片保活和 `eb44f662` sub2api 导入功能均保留。
- 整合后验证通过：严格 Clippy、Rust 格式、模型获取库 41 项、网关模型获取 20 项、Provider 查询 89 项、前端类型检查、sub2api 前端 6 项、Python 验证脚本 3 项和差异空白检查。
- 已创建审查记录提交 `4423c0cf docs(plan): 记录模型目录审查结果`，并将分支 `codex/fix-codex-lite-tools` 推送至 `origin`。
- GitHub Actions `Build App Image` 运行 `29356085297` 构建成功，生产镜像对应提交 `4423c0cff7ef92b8c7b824a00406c8a3cbbd1a1a`。
- 已通过现有 CI 产物部署流程更新 hd0526 的 `frontdoor` 和 `background`，两个服务首次和第二次检查均为 `healthy`，公开健康接口返回 `status: ok`。
- 生产启动后使用真实 Codex 账号检查版本，客户端版本从 `0.144.1` 自动更新为 `0.144.4`，数据库中的下一次检查时间已正常写入。
- 已强制刷新 Plus、Pro、Team 各一个有效账号的上游模型目录；三类账号均返回 `gpt-5.6-luna`、`gpt-5.6-sol`、`gpt-5.6-terra`，模型数分别为 7、10、7。
- 第二次健康检查时，两个容器已连续健康运行 9 分钟；最近 15 分钟未发现 `panic`、`fatal` 或 `error` 日志。

---

## 2026-07-14 sub2api JSON 导入兼容性

- 已收到 sub2api 导出 JSON 和 Niffler 前端“无法解析输入内容”截图。
- 已确认文件是有效 UTF-8 JSON，顶层为 sub2api 批量导出对象，包含 12 个 OAuth 账号。
- 已定位前端报错位置，正在检查 `parseImportText` 接受的具体结构及后端批量导入协议。
- 已确认前端和后端都缺少 sub2api 顶层 `accounts` 包装对象适配。
- 已核对 sub2api 源码，该 JSON 是其正式导出协议，不是损坏文件。
- 已确认 12 条凭证均可映射到 Niffler 现有 Codex Access Token 导入模型。
- 已完成根因、修复边界、字段处理和测试范围梳理；本轮未修改业务代码或生产数据。
- 已再次复核重复检测、记录命名和临时令牌生命周期，发现并修正了“外层名称映射为 account_name”这一遗漏。
- 用户确认号池名称使用“邮箱 · 空间名称”，缺少空间名称时使用“邮箱 · 空间 ID”，不使用 sub2api 外层名称，也不添加 `codex_` 前缀。
- 已在独立分支 `codex/fix-sub2api-import` 新增兼容性设计记录，准备补充失败测试。
- 已完成前端格式识别、后端 `accounts[]` 展开、嵌套凭证映射、逐条失败统计和记录名称生成。
- 已补充 sub2api 正常导入、无效账号、缺少 `accounts`、前端批量分流和网关持久化测试。
- 用户提供的真实文件结构核对通过：12 条记录均为 OpenAI OAuth，均包含访问令牌、邮箱、用户 ID 和空间 ID；按新规则可生成 12 个唯一名称，且没有 `codex_` 前缀。
- 验证通过：前端 5 项组件测试、前端类型检查、后端 12 项 sub2api 相关测试、11 项批量解析测试、2 项既有 OAuth 批量导入回归测试、Rust 格式和差异检查。
- UI 静态审查命中该弹窗原有的交互入口密度和配色规则；本次没有修改可见布局、样式或文案，因此未扩大范围处理这些既有问题。
- 代码审查发现并修复两个边界问题：sub2api 识别不再仅凭单个元数据字段；缺少邮箱和用户 ID 的条目不再允许仅凭空间 ID 创建记录。
- 新增旧单账号 JSON 回归测试和空间 ID 单独导入拒绝测试；前端 6 项测试、类型检查、后端 13 项解析测试、13 项 sub2api 相关测试及既有 OAuth 批量导入测试均通过。

---

## 2026-07-14 线上生图超时

- 已确认客户端生图工具正常加载。
- 已检查 hd0526 的生产 frontdoor/background 日志。
- 已确认多个 Pro 账号在 `gpt-image-2` 链路上长时间等待上游响应。
- 正在核对请求记录、出站配置和超时后的账号切换行为。
- 已查询 rn01 生产库的 `request_candidates`：1 次成功、30 次在约 124.6 秒取消。
- 已确认大多数请求已收到上游响应头和持续 `keepalive`，问题在 Niffler 没有向下游输出任何字节。
- 已确认图片端点未使用独立代理，基础 HTTPS/HTTP2 连通正常，未鉴权请求 0.12 秒返回 401。
- 已完成代码核对：旧心跳实现被固定关闭，与 Cloudflare 120 秒超时共同导致本次失败。
- 已确认完整账号重试流程外层已有同步图片保活实现，生产配置已改为开启。
- 已将缺省配置和配置读取失败时的行为改为默认开启，并同步后台默认值、文案和架构文档。
- 已通过 7 项同步生图保活测试、Rust 格式检查、前端类型检查和差异检查。
- 已提交并推送 `fcc5a165 fix(image): 默认开启同步生图保活`。
- GitHub Actions 生产镜像构建成功，已部署 hd0526 的 frontdoor 和 background。
- 两次健康检查均通过；部署后真实生图返回有效 PNG，首字节 0.87 秒，总耗时 21.56 秒。

---

# 原任务：后台钱包检索、用户时间展示与前端表格体验优化

## 2026-06-12

### Phase 1: 上线前复核

- 已复核钱包检索和用户创建时间展示的代码改动。
- 已运行 `npm --prefix frontend run type-check`，结果通过。
- 已运行 `cargo check -p aether-gateway`，结果通过。
- 已运行 `git diff --check`，结果通过。
- 已按 UI workflow 读取 Web 后台重构规则，后续前端优化按现有设计系统和工具型后台处理。
- 首次全量前端测试失败，根因为 Node 25 的实验性 localStorage 对象缺少标准 Storage 方法。
- 已新增 `frontend/src/test/setup.ts` 并接入 `frontend/vitest.config.ts`。
- 已补充 SQLite 钱包用户搜索回归测试。
- 已确认失败的 3 个前端测试文件重新通过。
- 已确认全量前端测试重新通过。

## Test Results

| Test | Result |
|------|--------|
| `npm --prefix frontend run type-check` | 通过 |
| `cargo check -p aether-gateway` | 通过 |
| `git diff --check` | 通过 |
| `npm --prefix frontend run build` | 通过 |
| `npm --prefix frontend run test:run -- src/api/__tests__/client.spec.ts src/utils/__tests__/crossTabRefresh.spec.ts src/stores/__tests__/auth.spec.ts` | 通过，7 个测试通过 |
| `npm --prefix frontend run test:run` | 通过，82 个文件、415 个测试通过 |
| `cargo test -p aether-gateway public_support` | 通过，176 个测试通过 |
| `cargo test -p aether-gateway wallet` | 通过，75 个测试通过 |
| `cargo test -p aether-data sqlite_wallet_read_repository_reads_wallet_contract_views` | 通过，1 个测试通过 |

## Error Log

| Error | Resolution |
|-------|------------|
| `ui_workflow.py` 第一次运行缺少必填参数 | 补充 `--platform web`、`--surface`、`--goal` 等参数后重新运行成功 |
| 全量前端测试中 localStorage 方法不存在 | 在 Vitest setup 中安装标准内存 Storage 后通过 |
| SQLite 钱包搜索回归测试缺少导入 | 补充测试模块 import 后通过 |
# 2026-07-28 Telegram 监控通知与 Bot 设置

- 已核对两台生产服务器的监控配置、服务状态和 Telegram 更新方式。
- 已确认现有监控阈值可配置，但缺少面向管理员的设置入口。
- 已记录人性化文案、Bot 命令、单实例接收和跨服务器受限同步设计。
- 正在修改脚本并补充自动测试。
- 已将生产通知改为“服务器、系统盘、业务服务、影响和建议”的中文表达，并隐藏日常
  消息中的容器健康字段、SHA-256 和 R2 对象路径。
- 已在 `rn01` 启用唯一 Telegram 命令控制器，注册 `/status`、`/settings`、三个
  设置命令和 `/help`。
- 已使用新的受限 SSH 用户和强制命令同步 `hd0526`，拒绝任意额外远程命令。
- 已完成两台服务器部署；监控、备份测试、应用健康检查和隔离脚本测试通过。

---
# 2026-08-01 提供商测试、单账号测试与账号并发

- 已完成 Niffler 与 sub2api 的现有实现核对。
- 已确认单账号参数、Redis 并发令牌和账号列表响应的改动位置。
- 已新增设计记录 `docs/architecture/2026-08-01-provider-model-test-and-key-concurrency.md`。
- 下一步实现后端账号并发聚合、单账号参数传递和账号列表字段。

---

# 2026-08-03 Codex Responses 历史推理项 ID 兼容修复

- 已核对线上请求详情：错误为 `reasoning` 历史项使用 `item_*`，三次账号尝试均返回相同 HTTP 400。
- 已核对 OpenAI 官方文档与官方 Codex 源码，确认 `reasoning` 项固定使用 `rs_*`。
- 已定位本地 Codex Responses 请求转换入口和直接 HTTP 400 的账号切换路径。
- 已记录目标、非目标、影响范围和验证方式；下一步先补失败测试。
- 推理项 ID 回归测试先失败，确认旧实现会原样发送 `item_*`；加入限定转换后测试通过。
- 直接 HTTP 400 回归测试先失败，确认旧实现会继续切换账号；加入 Responses 参数错误停止规则后测试通过。
- 已验证普通 OpenAI 提供商不执行 ID 转换，普通 Chat Completions 参数错误仍沿用原行为，容量错误仍可切换账号。
- 已完成相关测试：格式转换 8 项、Responses 参数错误 2 项、直接 HTTP 400 1 项、容量错误 2 项均通过。
- `cargo fmt --all --check` 和 `git diff --check` 通过；本次未提交、未部署。
# 2026-08-10 提交构建前最终复审

- 用户要求重新审查代码；只有没有阻断问题时才创建提交并构建。
- 已读取当前计划、发现和进度记录，确认本轮继续使用文件化检查记录，并按约定式提交规则执行条件提交。
- 已复核工作区范围、差异统计和最近提交风格；当前有 88 个修改或未跟踪文件，计费与双入口任务混在同一工作区。
- 已重新确认三种数据库结算实现仍保留“缺少请求扣费决定记录时按旧规则扣钱包”的逻辑，SQLite 还有错误行为测试；当前已判定不能提交，继续复审其他生产阻断项。
- 已检查请求前资金读取、请求扣费决定写入和 PostgreSQL 套餐额度查询：新增顺序数据库往返且存在按权益/额度窗口逐条查询，违反本次设计文档的性能要求。
- 已检查自动化测试覆盖：没有生产 PostgreSQL 新扣费业务路径的集成测试，不能用 SQLite 通过代替上线验证。
- 已复核钱包正数/零/负数、适用套餐、负钱包购买和单生效套餐等入口，主要业务判断已经接入。
- 已发现请求扣费决定冲突校验漏比实际供应商字段，记录可能不准确；继续检查套餐钱包联合结算、迁移和界面显示。
- 已确认套餐扣到 0、剩余费用扣钱包以及重复结算锁的核心算术方向正确。
- 已确认缺少请求扣费决定记录时没有完整的保留费用、重试和处理流程；同时请求扣费决定表没有清理或归档任务。
- 已复核管理端用户列表和钱包中心：真实负余额已显示，但钱包中心会对所有欠费用户错误提示“套餐仍可使用”。
- 已复核 PostgreSQL 迁移：只创建新表，没有现有套餐/权益供应商关系的生产补录步骤；直接部署不能解决现有权益缺少 `gpt-5.6-sol` 的问题。
- 已发现套餐管理中英文说明仍宣称不同套餐可以并存，与本次单套餐规则冲突。
- 已确认新套餐管理页使用供应商选择并动态显示模型，方向正确。
- 已确认现有用户套餐编辑接口不能修改供应商范围，当前也没有批量补录工具；旧套餐迁移仍未真正落地。
- 已复核 PostgreSQL 套餐到账并发处理：通过同一用户的钱包行锁串行检查重叠套餐，单生效套餐规则的并发保护成立。
- 已运行旧扣费回退测试并确认它通过，证明错误行为仍被测试明确保留。
- 已运行 `cargo fmt --all -- --check`，结果通过。
- 已定位新增顺序 PostgreSQL 查询的准确代码范围，并与设计文档性能要求逐项核对。
- 已定位 PostgreSQL CI 工作流，确认没有覆盖新扣费业务行为。
- 已完成 Phase 19 复审并确认阻断问题；`git diff --check` 通过，本轮按用户条件未提交、未构建。
- 用户要求修复复审问题，已进入 Phase 20；先更新架构文档，明确缺少请求扣费决定时保留费用、不猜测扣款，以及固定次数 PostgreSQL 查询、旧套餐补录、记录清理和生产测试要求。
- 已确认请求正常路径的报告数据已经携带本次扣费决定，因此可以删除请求开始前额外读取数据库的步骤，不需要重新读取余额。
- 已确认终态使用记录在结算失败后仍保留 `pending` 和实际费用；现有超时清理只处理请求状态仍为 pending/streaming 的记录，不会自动作废已完成但待结算的费用。
- 已删除 PostgreSQL、MySQL、SQLite 在缺少请求扣费决定时按钱包或用户重新猜测扣款的旧逻辑；现在结算停止、事务回滚，钱包不变，实际费用继续保留为待结算。
- 已将保护错误透支行为的 SQLite 测试改为验证：缺少请求扣费决定时不扣钱包，并保留实际费用和 pending 状态。
- 已补上请求扣费决定的实际供应商一致性校验。
- 已删除上游调用前单独读取请求扣费决定的数据库查询；正常请求直接使用规划阶段随报告数据携带的决定。
- PostgreSQL 请求扣费决定写入改为一条 `INSERT ... RETURNING`，冲突时原值不变并直接返回，不再插入后再次查询。
- 已补充 PostgreSQL 实际结算测试，覆盖缺少决定记录、并发重复结算和套餐加钱包补差。
- 已清理旧架构文档中“不同套餐可以并存”的冲突规则，统一为单一有效套餐。
- 首个 SQLite 回归测试暴露测试数据断言错误；已修正余额拆分断言，并为正常扣款测试补齐请求开始时保存的扣费决定记录。
- 已修正 PostgreSQL 静态测试的自我命中问题；前四项后端定向测试中三项已通过，失败项修正后待复跑。
- 结算模块 29 项测试运行后，23 项通过、6 项旧测试因缺少请求扣费决定失败；已给这些场景补上明确的钱包或套餐决定，待复跑。
- 修正测试数据后，`cargo test -p aether-data repository::settlement --lib` 全部通过：29 项通过、0 项失败。
- 网关去重查询测试通过，`cargo check -p aether-gateway` 通过，前端 `vue-tsc --noEmit` 通过。
- 正在增加结算失败后的数据库持久重试：启动时和每五分钟处理已结束但仍待结算的请求，失败继续保留费用并按请求 ID 报错。
- 已接好三种数据库和内存实现的待结算批量读取接口，下一步接入现有五分钟维护任务并补测试。
- 后台重试已接入五分钟维护任务；定向测试确认会读取已结束且待结算的请求并重新执行同一结算逻辑。
- 三种数据库的扣费决定清理均已限制为“已结算、无服务尝试记录、超过保留期”；SQLite 行为测试和三种 SQL 约束测试通过。
- 套餐供应商页面定向测试通过。
- 真实 PostgreSQL 并发测试发现重复结算的第二个返回结果缺少钱包余额；已定位为等待行锁时使用旧数据视图，并改为锁释放后重新读取完整结算结果。
- 并发返回修复后的 PostgreSQL 测试已进入套餐拆分场景；修正超长测试订单 ID 后待再次运行。
- 真实 PostgreSQL 测试最终通过，包含迁移、缺记录不扣款、并发幂等、套餐与钱包补差，以及批量套餐额度查询前后的金额变化；独立测试数据库已删除。
- 管理员修改旧套餐供应商范围的接口测试通过，返回结果确认保存了供应商 ID。
- 最终路由复查发现旧套餐会缺少扣费决定，以及套餐覆盖当前模型时仍可能切到套餐外供应商；已先更新架构规则，正在修复模型级查询和供应商限制。
- 模型级查询和旧套餐扣费决定已完成定向测试；继续处理分批读取服务时的套餐适用判断，防止当前批次没有套餐供应商便误用钱包。
- 已改为按当前模型查询结果判断套餐是否适用，不再依赖当前服务批次；当前批次没有套餐供应商时会继续下一批。相关 7 项路由测试全部通过。
- 已在分批服务读取器中按全局模型保存本次请求的钱包和套餐结果；同一模型的后续批次直接复用，不再重复访问数据库。正在编译并补回归验证。
- 网关编译通过；旧逻辑残留搜索和差异格式检查通过。开始运行数据层全量测试并继续检查核心金额边界。
- 数据层全量 525 项测试通过。继续复查发现跨供应商重试冲突和套餐越界改扣钱包两个阻断问题，已先修正文档，正在修改付款决定和结算校验。
- 已移除付款决定对首个供应商的依赖，允许同一请求在套餐供应商范围内切换；已删除套餐越界改扣钱包逻辑，并增加范围内重试、范围外拒绝、旧套餐兼容和套餐外模型钱包支付测试。正在复跑定向测试。
- 已从新付款决定数据结构和三种数据库迁移中彻底删除无用的单个供应商字段；保留现有使用记录中的最终供应商字段。修正一次测试文件误删后继续复跑。
- 已删除内存结算按用户/API Key 猜钱包的旧逻辑，相关单元测试和网关测试改为显式提供请求开始时的钱包决定；准备重新运行数据层全量测试。
- 结算 31 项测试和网关钱包端到端测试通过；全新 PostgreSQL 数据库上的真实结算测试也通过，测试数据库已删除。
- 已复查用户列表、钱包详情和钱包中心的负余额展示：列表显示真实负值，接口区分真实余额与可消费余额，欠费提示会按套餐是否仍有余额显示。
- 已复查服务端套餐购买和到账逻辑：欠费、其他生效套餐及并发待支付订单均在事务内复核；同套餐续费和管理员赠送按已确认规则处理。
- 已删除三种数据库结算中不可达的“套餐供应商越界改成钱包”旧分支；实际越界仍在扣款前返回错误并完整回滚。
- 已确认金额预占已在运行时强制关闭，旧开关无法重新开启；历史表和管理接口仅为兼容保留。
- `cargo fmt --all` 和 `cargo test -p aether-data --lib` 通过：527 项通过，0 项失败。
- `cargo test -p aether-gateway --lib` 通过：2,983 项通过，0 项失败。
- `npm --prefix frontend run type-check` 和 `npm --prefix frontend run test:run` 通过：112 个测试文件、572 项测试全部通过。
- `npm --prefix frontend run build` 通过。
- 全新本地 PostgreSQL 数据库上的真实结算测试通过，临时数据库已由退出清理自动删除。
- 严格 Clippy 首次仅有三个测试动态数组告警，已修正，准备复跑。
- 严格 Clippy 复跑通过；PostgreSQL 临时测试库清理结果为 0 个残留。
- 已复查三种数据库迁移字段、外键和索引，并确认 CI 会执行新的 PostgreSQL 计费行为测试。
- 已确认语言文件存在跨任务混改；计费文案在独立合并层完整提供，提交时排除两个混合语言文件及全部线路测速文件。
- 已创建提交 `ae5b8d6e5`（`fix(billing): 根治有限钱包持续透支`），只包含计费修复。
- 已从该提交的独立工作区完成前端生产构建；`cargo build -p aether-gateway --release` 通过。
- 2026-08-10 开始实现欠费用户购买套餐合并付款；已恢复上一阶段记录，并确认本轮不能沿用“欠费禁止购买”的旧规则。
- 已读取 `using-ui-polish` 和 `planning-with-files` 完整规范；下一步生成现有购买页改造约束并检查订单与到账代码。
- 已生成现有购买页的界面改造约束，并读取 UI 核心规则；界面主任务确定为先看清实付总额和金额组成，再确认付款。
- 已读取界面硬性规则和执行规则，金额区将沿用现有样式并以紧凑明细呈现，不增加新卡片层级。
- 已读取现有 Web 平台和整体设计规则；下一步读取文案与操作反馈规则，然后进入代码和数据结构检查。
- 已读取付款文案与操作反馈规则；界面规范准备完成，开始核对订单表、创建订单、到账事务和购买页面。
- 已确认设计文档和四层实现仍执行“欠费拒绝购买”；已定位订单创建与到账实现，下一步缩小范围读取金额字段和事务细节。
- 已定位钱包订单类型、三种数据库迁移和两条用户套餐付款入口；继续读取完整字段与事务实现。
- 已确认当前订单金额只等于套餐价，数据结构没有欠款金额；继续检查到账、退款和订单返回，确定金额字段必须如何拆分。
- 已确认金额组成需要持久保存，且付款平台必须使用数据库订单返回的实付金额；正在检查到账和退款事务。
- 已确认支付回调按订单总额验款、套餐订单现行不可退款，并定位自动到账与手动到账两条发放路径；继续读取事务中的钱包和幂等处理。
- 已读完 SQLite 创建订单、自动到账和手动到账事务；金额记录、还款和套餐发放可以在现有幂等事务中完成。下一步核对钱包金额桶、三种数据库锁和前端数据流。
- 已确认欠款落在充值余额，PostgreSQL 下单已锁定钱包；初步数据设计为订单新增欠款金额，订单总额表示实付金额。继续核对前端付款流程和全部订单映射。
- 已确认欠费购买需要取消前端禁用，并在创建订单后暂停自动跳转，先展示服务端确定的金额组成；继续核对订单公共类型和现有测试。
- 已核对前端订单类型和测试现状；准备更新设计文档，并在实现前先补数据库与组件失败测试。
- 已更新架构文档，完整替换“欠费禁止购买”旧规则，写明订单金额、到账还款、并发新增欠款、幂等、管理员赠送、界面和性能要求。
- 已找到可直接改造的内存负钱包测试和 SQLite 套餐到账测试，开始先写新行为断言并确认旧代码失败。
- 已确定数据层失败测试的两个关键场景：下单时已有欠款的合并付款，以及下单后新增欠款不让已付款套餐失败。
- 已先运行两条数据层失败测试，均因旧的欠费拒绝分支失败，证明测试能捕获本次行为变化；开始实现订单字段、金额计算和到账还款。
- 数据层红灯已确认；补前端欠费购买红灯测试后再进入实现。
- 已确认前端测试挂载和异步等待写法，开始新增套餐页欠费购买测试。
- 前端测试依赖和组件替换方式已确定，开始写测试文件。
- 已新增欠费购买组件测试并确认旧界面行为不符合新需求；测试工具本身的问题已修正，现在失败来自购买按钮仍被禁用。
- 数据层和前端失败测试均已确认，进入实现阶段。
- 已核对订单公共类型、三种数据库查询常量和所有测试订单构造位置，开始增加迁移与公共金额计算。
- 已增加三种数据库迁移、订单欠款字段和统一金额计算函数；正在接入三种数据库下单与订单映射。
- 三种数据库下单与订单映射已接入，数据层非测试代码编译通过；正在实现自动到账和手动到账的原子还款。
- 三种数据库自动与手动到账的原子还款已接入；测试首次编译发现一个样本缺字段，已修正并继续复跑。
- 数据层两条核心新行为测试均已通过；下一步改网关两条付款渠道统一使用订单返回总额，并返回金额组成。
- 已核对两条付款渠道的准确顺序，开始修改 DodoPay 与 ePay 使用订单总额并删除欠费拒绝分支。
- DodoPay 与 ePay 已统一使用数据库订单返回的实付金额；ePay 会将准确付款参数写回订单，写入失败会取消本地订单并停止付款。
- 三种数据库和内存实现已完成订单欠款字段、合并金额、到账还款和重复到账保护；网关编译及测试编译通过。
- 欠费购买页面已取消购买禁用，订单创建后显示套餐价格、钱包欠款和本次实付，并等待用户点击“前往付款”；无欠费订单保持自动打开付款。
- 页面专项测试 2 项通过，数据层套餐购买相关测试 7 项通过。
- 全新本地 PostgreSQL 数据库中的真实欠款下单、到账和重复到账测试通过；临时数据库已删除。
- 前端全量类型检查被 14 处本次范围外的既有错误阻断；继续进行本次文件静态检查、生产构建和 UI 复核。
- 前端全量测试通过：113 个测试文件、574 项测试全部成功；目标套餐购买组件测试 2 项再次通过。
- 数据层全量 529 项和网关全量 2,983 项测试全部通过；套餐购买相关 8 项定向测试再次通过。
- 前端生产构建和目标 ESLint 通过；生产构建只有既有的浏览器数据过旧和大分块提醒。
- Rust 自动格式化完成，`cargo fmt --all -- --check`、相关包全目标严格 Clippy 和 `git diff --check` 均通过。
- 欠费购买页面已完成静态与真实运行时复核，结果为 `PASS ui-review gate`；临时页面、临时服务和临时 PostgreSQL 数据库均已清理。
- 最终差异复核确认：欠款金额由下单事务固定，DodoPay 与 ePay 使用订单实付总额，自动回调与手动到账都只还款一次并在同一事务发放套餐；没有恢复欠费钱包继续按量请求的能力。
- 本轮实现和验证完成；未暂存、未提交、未推送、未部署，也未修改生产钱包或历史账单。
- 欠费合并付款提交 `99615afa1` 已通过 SSH 推送到远端同名分支；HTTPS 首次推送因令牌缺少 `workflow` 权限被拒绝，改用本机已验证的 GitHub SSH 身份后成功。
- 手动运行远端 `Build App Image`，前端、Linux 网关、部署脚本和 amd64 镜像构建全部通过；镜像产物有效至 2026-08-18 01:58，测试部署步骤按非 test 分支规则跳过。
- 用户要求合并到 main；已确认必须先整合远端 test 的独立提交，再通过 test → main 晋级，当前进入独立工作区合并阶段。
- 已从最新 test 创建独立发布分支并合并计费分支，无冲突；格式和差异检查通过，创建 test 合并请求 #34。
- #34 首次 Frontend 检查被依赖审计阻止；已将锁文件中的 DOMPurify 和两份 nanoid 更新到修复版本，生产依赖审计恢复为 0 漏洞。
- 锁文件修复已推送；第二轮 Frontend、Release tooling、Format 和 Clippy Data 已通过。等待其余检查时 GitHub 查询短暂返回 503，检查任务未报告失败。
- 2026-08-12 开始 Niffler 与 Aether 全量分叉审计。已纠正此前错误比较范围，固定真实共同祖先、已发布 Niffler 主线和 Aether 上游主线。
- 已读取 `planning-with-files` 完整规范并恢复现有计划文件；现有计划文件和工作区改动保持不动，本次新增独立审计目录。
- 已新增 `docs/analysis/aether-upstream-audit-2026-08-12/00-audit-charter.md`，记录目标、非目标、比较对象、完整性口径、分类、处置标签、证据等级和四轮复核规则。
- 当前进入机器清单生成阶段；尚未修改业务代码、合并上游、执行迁移或部署。
- 机器清单脚本首次运行在解析 `git diff --name-status -z` 时提前失败，没有生成不完整附件；已确认 NUL 模式下状态和路径是独立字段，正在修正解析与校验。
- 已修正清单脚本并生成 3310 条路径、357 条 Niffler 独有提交、776 条 Aether 独有提交及每提交影响附件。
- 已用独立 Git 命令交叉验证路径交集、并集和最终树差异，数量全部一致；Phase 2 完成。
- 已新增 `02-inventory-verification.md`，明确重命名误配限制和后续人工审计覆盖要求。
- 当前进入 Niffler 316 条非合并提交的功能簇审计；尚未做任何业务代码合并或修改。
- 覆盖复核发现本地仓库是浅克隆，补全 `origin` 历史后重建了逐提交影响、分类、路径映射、覆盖和处置账本；Niffler 357 条、Aether 776 条均实现一一对应。生成器现会拒绝在浅克隆中运行。
- 第二次覆盖复核发现路径来源脚本跳过合并提交，会漏掉仅通过合并结果进入主线的路径来源；现已改为统一记录提交相对第一父提交的差异，并将全部合并提交固定标为 `integration_merge/HISTORY_ONLY`。3,310 条最终路径均无来源缺口。
- 处置账本已增加提交级例外：Niffler 真实金额预占标为 `SPLIT`；Aether 的 Redis 持久化默认关闭、数据库池默认下限 32、两条后台在线更新/部署提交标为 `REJECT`；Redis 连接、数据库压力和热点治理标为 P0 语义吸收。
- 一致性校验第二次运行发现“该侧历史改过但最终恢复”与“该侧从未改动”共用同一状态；已将前者单列为 `historical_only`，避免把历史触达误说成最终差异来源。校验在修正前停止，没有产生虚假的通过记录。
- 技术反证复核纠正了一处路径表述：Redis 治理最初引入在 `crates/aether-runtime-state`，Aether 最终分层路径是 `crates/aether-runtime/state`。报告已同时记录历史位置与最终位置，避免实施时复制错层。
- 生产风险复核完成迁移目录比对：Niffler 134 个 SQL/62 个版本，Aether 109 个 SQL/58 个版本；三数据库共同基线版本内容均不同，而现有校验只警告 checksum 漂移。路线图新增真实 schema 目录对账，明确禁止覆盖迁移目录或只凭版本判断兼容。
- 四轮复核完成。最终从固定 Git 历史重跑全部生成器、Python 语法检查、覆盖校验和 `git diff --check` 均通过；3,310 条路径和 1,133 条双向独有提交全部有来源、分类和处置。未修改业务代码，未执行迁移、合并、推送或部署。
- 已增加可重复执行的迁移碰撞附件生成器；最终结果为 Aether 独有 42 组、Niffler 独有 67 组、内容一致 64 组、同版本不同内容 3 组，和人工生产风险复核一致。
## 2026-08-12：进入 Niffler Core 最终态调用链审计

- 已完成：固定共同基点和双方目标版本；生成无重命名推断的全路径清单、双方提交清单、路径交集和冲突等级汇总。
- 当前：从 `origin/main` 反查 Niffler Core 的最终运行时入口、数据读写和管理端可达性。
- 下一步：形成 Niffler Core 子系统判定表，再审计计费、OAuth 导入、流式故障切换、公开站点和运维改造。

## 2026-08-12：完成路径—提交反向索引

- 已生成 `generated/niffler_path_commit_map.tsv` 和 `generated/aether_path_commit_map.tsv`。
- 已生成 `generated/niffler_cluster_paths.tsv` 和 `generated/aether_cluster_paths.tsv`。
- 这些文件覆盖最终仍有差异的路径，也覆盖分叉期间曾修改但最终恢复/删除的历史路径。

## 2026-08-12：覆盖账本生成错误

- 首次生成 `path_coverage_ledger.tsv` 时遗漏了 `gateway_other` 到语义报告章节的映射，脚本以 `KeyError` 停止，没有生成不完整结果。
- 已补映射后重新运行；该错误不影响已有提交和路径清单。

## 2026-08-12：覆盖账本已生成

- `generated/path_coverage_ledger.tsv` 共 3,310 条数据记录，全部标记为 `mapped`。
- 下一步：审计当前工作区尚未发布的 Niffler 改动，并形成实施顺序和回滚门禁。
# 2026-08-13 Review Fix 继续实施

- 已确认上一轮 `codex_identity_convergence.rs` 补丁仅部分写入：延迟读取配置和新入口已存在，客户端身份、父子任务改写及测试仍是旧接口。
- 当前文件尚未形成可编译结果；后续按核心模块、调用点、管理端测试、验证四步修复，不覆盖工作区其他改动。

# 2026-08-14 Codex App 生图未展示诊断

- 已核对用户截图和现有图片架构文档：截图中没有任何真实生图工具调用，当前现象是模型错误宣称成功。
- 正在核对客户端工具加载条件、服务端托管工具注入逻辑和生产请求记录；未修改业务代码、生产配置或部署。
- 已确认托管工具的配置优先级：Provider/Endpoint 显式 `false` 会覆盖 Codex 默认开启值。运维文档记录生产“Pro号池”曾保持该关闭值，现在继续核对当前生产配置和请求事件。
- `rn01` 生产 PostgreSQL 与 Redis 容器健康，已获得只读核对条件；下一步直接查询 Provider/Endpoint 当前图片工具开关。
- 已直接确认生产三个 Codex 号池的图片工具开关均为有效开启，端点也处于启用状态；旧的 Pro 号池关闭值已被更正，排除为当前根因。
- 开始核对近期请求是否声明了客户端 `image_gen`，导致网关有意跳过托管 `image_generation` 注入。
- 用户提供了生产用户 `wenwen`、大约 16:39 的测试时间和历史有效的本地 Provider 配置。OpenAI 官方文档已确认这两个配置字段的当前语义；接下来按该用户和时间直接查请求记录。
- 已确认 `usage` 中有原始和最终上游的请求头/请求体，可以不查看其他用户内容，只对 `wenwen` 做定向比较。
- 已定位当天 15:15–15:16 的三个 `wenwen` 测试请求；系统全部当作 HTTP 200 普通响应完成，不存在图片工具失败事件。开始读取这三次的入站/出站请求头和工具类型。
- 生产当前版本尚没有本地新 schema 中的 body reference 列；首次查询在未知列处停止，没有改动数据。已改按生产实际 inline/compressed 列查询。
- 一次同时计算多个大 JSON 字段长度的查询没有及时返回；已停止这种扫描方式，改为按单个请求、单个字段定向读取。
- 定向查询确认目标请求的全部 body/header 字段已被生产隐私清理，不再尝试从数据库还原原文。下一步按 request ID 查生产容器日志。
- 已确认主入口当前落在 `hd0526`，首先查该节点的 Frontdoor 日志；同时核对本机 `cpa` Provider 的实际基础地址和两个关键配置值。
- 已直接确认本机 `cpa` 当前配置错误：`requires_openai_auth` 为 `true`，且缺失整个 `http_headers` 表。这两项恰好是用户所述的历史修复条件，当前复发已有直接配置证据。
- 继续检查配置文件修改时间、备份、当前活动 Provider 和最近生产提交，区分“本地配置被重写”与“网关收到后删除请求头”。
- 已确认生图测试时实际活动 Provider 是 `custom`，不是 `cpa`；两个 Provider 都缺失图片授权请求头且要求 OpenAI 身份验证。当前配置于今天 14:06 改写，在测试前已生效。
- 已从 `hd0526` 容器日志确认三次请求均为普通 Responses 成功请求。继续核对配置改写来源和最近身份收敛代码对该请求头的处理。
- 已排除最近身份收敛代码删除 `X-OpenAI-Actor-Authorization`；该请求头不在删除名单或前缀中。
- 已确认用户配置生成出口不一致：公共首页与指南仍生成 `requires_openai_auth = true`，安装脚本生成 `false`，三者都不会添加本地生图所需的 Actor Authorization 请求头。这是导致手工修复被后续配置更新覆盖的产品级缺口。
- 已确认本机正在运行 CC Switch，且没有其他 Codex 任务或 Codex 进程在 14:06 附近改写配置的证据。开始查 CC Switch 本地 Provider 存储是否缺失两个生图字段。
- 已用 `git ls-remote` 确认生产镜像提交与远端 `main` 一致；下一步只读获取该提交并对照最终代码。
- 已获取并对照生产提交；该版本没有修改图片、Codex 请求头或配置生成逻辑，排除为直接回归来源。
- 已定位 CC Switch 主数据库和日志路径，接下来只读检查 Provider 数据和 14:06 附近的切换记录，不输出密钥。
- 已完成 CC Switch 当前数据库与多份备份的脱敏对照：不同 Niffler Provider 中的两个生图字段长期不一致，切换到错误模板会稳定覆盖已修好的全局配置。
- 开始读取三个目标请求的非正文元数据和工具可见事件，确认服务端最终是普通文本链路。
- 生产请求核对完成：目标请求来自 Codex Desktop 26.727.51351，只有普通 Responses 尝试，`image_count = 0`，无图片工具/能力标记和图片计价。开始查 `wenwen` 最近一次真实 Images API 调用及当时客户端版本。
- 历史成功对照只查 `wenwen` 的接口类型、图片计数和客户端版本，不读取请求正文或其他用户内容。
- 已确认生产主库容器和 `usage`/`users`/`request_candidates` 实际表名，开始用只读 SQL 筛选图片计数大于 0 或 Images API 路径的历史记录。
- 更正：首次图片明细查询超过单次输出窗口，其空输出不代表零行。聚合复核确认 `wenwen` 有 119 条图片路径记录、135 条 `gpt-image-*` 模型记录，开始改用窄查询取最近成功样本。
- 已找到最近成功样本：8 月 3 日 22:19:22 进入 `/v1/images/edits`，由 Pro号池 `gpt-image-2` 完成；8 月 3 日晚间还有多条同类记录，之后不再出现图片模型请求。
- 已核对成功图片与失败测试的元数据字段：两者都保留 `user_agent`、`request_path` 和计费维度；失败测试还保留 Codex `client_family`。下一步只取这些安全字段比较客户端版本。
- 成功与失败请求都来自同一 Codex Desktop 26.727.51351，排除客户端版本升级；成功时 Responses 后会紧接 `/v1/images/edits`，失败时只有 Responses。
- 成功和失败样本的原始请求头/请求体都已被隐私清理，无法从生产库逐字还原 Actor Authorization 请求头；最终结论将明确区分直接证据和推断。
- 已审查成功后第一个修改 Responses 图片桥接文件的 8 月 4 日提交：只改桥接指令保留/去重与兼容性，没有改工具启用或 Images 路由。
- 重新确认 CC Switch 当前数据库的 Codex Provider 清单：只有 11 份其他 Niffler 配置，没有 `Niffler - wenwen`。开始逐份检查 7 月 31 日至 8 月 14 日备份是否曾保存该名称。
- CC Switch 所有当前/备份数据库都没有 `Niffler - wenwen`；Codex Desktop 日志只证明当天全局 `image_generation` 功能标志已开，没有保留该对话的本地图片工具加载详情。
- 用户纠正：`wenwen` 电脑的实际配置包含 Actor Authorization，`requires_openai_auth = false`；之前把排查者本机配置当成 `wenwen` 证据的判断错误，已从结论中删除。现在以服务端回归为唯一排查方向。
- 已定位服务端工具互斥分支：客户端 `image_gen` 一旦出现，网关就不注入托管 `image_generation`。8 月 4 日新受管理提示词只识别后者，开始查该分支引入时间和 `wenwen` 用户组配置。
- 互斥分支在 7 月 11 日已存在，不是本次新回归。成功与失败请求的 API Key 用户组 ID 相同，开始查该组在 8 月 4 日后是否启用了受管理提示词。
- `wenwen` 用户组的受管理提示词为空，排除该功能。已冻结成功基线 `621c53ef1` 和当前生产 `9f2959a28`，开始按日聚合全站 Images 请求，定位中断日期。
- 首次全站聚合同时解析 JSON 和多个 OR 条件，120 秒超时；数据未修改。改为先检查索引，再只按 `request_type = 'image'` 查图片记录。
- 已确认可用 `(api_family, endpoint_kind)` 索引，图片行的 `endpoint_kind = image`。全站统计改用该索引，不再解析大 JSON。
- 全站统计确认 8 月 13‑14 日仍有 75 次成功图片请求。同一用户组、Pro号池的 Windows Codex Desktop 在 `wenwen` 失败后仍成功调用 Images API；开始核对 `wenwen` 所用 Mac 客户端版本的最后成功时间。
- 同版本客户端全站在 8 月 13 日仍能生图，但 8 月 4 日的同版本 Mac 成功样本不属于 `wenwen`；已更正。后续只比较 `wenwen` 8 月 3 日成功与 8 月 14 日失败的元数据。
- `wenwen` 同用户对照确认所有稳定路由字段一致，唯一有效服务端差异是选中了不同的 Codex OAuth 上游账号。开始核对失败账号是否有其他成功生图记录。
- 另一成功链的 Responses 使用第三个 OAuth 账号。对目标账号做全量时间自连接超时，已改为直接查账号配置、候选能力和少量近期样本。
- 失败账号本身处于正常活动状态，无模型/API/能力限制、OAuth 失效或最近错误；与同日成功 Responses 账号的结构化配置一致。继续核对上游元数据和请求候选记录。
- 两个账号的 Codex 上游元数据字段集合一致；继续比较 plan type、credits 和限额窗口值，然后核对候选记录。
- 账号元数据和候选记录除 OAuth 账号外无有效差异。开始逐步追踪 `X-OpenAI-Actor-Authorization` 从入站到最终 ChatGPT Codex 上游的请求头构造。
- 仓库内没有 Actor Authorization 的专用处理或测试，它完全依赖通用透传。已定位到 `aether-provider-transport` 的标准请求头构造函数，开始读实现和 Git 历史。
- 通用 same-format 透传不会删除 Actor Authorization；开始检查传入该函数前的 `effective_headers`、Endpoint header rules 和最后身份收敛。
- `effective_headers` 只有“原始入站头”和“路由组变换后的头”两个来源；开始查路由变换创建点。
- 路由变换从原始入站头完整克隆开始，只有显式 mutation plan 会改头；开始查目标请求的 routing trace 和 Endpoint `header_rules`。
- Pro Endpoint 无 header rules，最后身份收敛也保留 Actor Authorization。现有服务端代码不会在收到该头后丢弃它；开始核对图片工具开关在目标请求时的配置时间。
- 重要更正：Pro Provider 当前图片开关虽为 `true`，但记录在 17:17 被更新，晚于 `wenwen` 15:15 失败测试。已停止用当前值代表当时值，开始查 `audit_logs` 的 Provider 变更记录。
- `audit_logs` 在相关三小时窗口为空，不能还原 17:17 更新。改查 Frontdoor 访问日志和 15:15 前的最近数据库备份。
- 本机 Codex 任务文件中对截图提示词的搜索只命中当前诊断任务，没有找到原测试对话；继续查 Codex Desktop 应用记录，不将当前任务误当成原请求。
- 已停止一次范围过大的 Frontdoor 日志查询；该查询没有形成有效结论。接下来精确检查 17:17:21 本地时间对应的管理接口写请求，必要时读取 15:15 前的数据库备份确认当时开关值。
- 已确认普通 Provider 更新使用 PATCH 管理接口；在 17:17:21 前后 40 秒没有找到对应 Frontdoor HTTP 完成记录。继续扩大到 17:15–17:20 的精确写请求过滤，并检查备份。
- 17:15–17:20 仍无 Provider 管理写请求日志。已确认当天 04:35 有一份测试前数据库备份，开始从备份中只读取 Pro Provider 记录。
- 已确认本地备份文件已清理、R2 对象仍在；准备通过现有 PostgreSQL 容器流式读取归档目录，整个过程不连接或写入生产数据库。
- 已成功读取备份归档目录，确认 `public.providers` 表数据可单独提取；开始读取 Pro Provider 的单行历史值。
- 单表流式提取第一次缺少输出参数，补正后仍为空；暂不下结论，开始核对归档流限制。
- 改用正确表名后成功提取目标历史行：15:15 前图片工具开关已为 true。已排除“当时服务端开关关闭”，开始按三个目标请求 ID 检查工具注入和响应事件日志。
- `docker logs` 扫描目标时段过慢，已停止且未形成结论；改为直接搜索容器日志文件中的三个请求编号。
- 已从容器原始日志精确找到三次请求：全部正常完成 Responses 请求，日志没有记录最终工具清单。开始检查服务端是否在使用“客户端已声明本地图片工具”条件时跳过托管工具。
- 已确认该跳过条件存在：声明本地 `image_gen` 会禁用服务端托管图片工具及强制调用指令。开始比较 8 月 3 日成功与 8 月 14 日失败请求中可恢复的工具相关元数据。
- 已排除开关传递链异常；现有审计没有单独保存工具清单。下一步检查失败所用上游 OAuth 账号是否在其他图片任务中也表现异常。
- 已确认 wenwen 使用失败账号的 56 次 Responses 后都没有 Images 请求，但此前未必都是图片任务，不能据此判定账号异常。改从 8 月 14 日全站成功图片任务反向查其前置 Responses 账号。
- 全站反查确认同一上游账号在 wenwen 失败前后均成功触发图片请求；已排除账号能力问题。继续比较紧邻成功样本与 wenwen 失败样本的客户端和请求元数据。
- 紧邻成功样本来自新版 Windows 客户端，不能替代 wenwen 的旧版 Mac 请求证据。开始审查 8 月 3 日成功基线到当前生产之间所有会改变 Codex 请求体或身份请求头的代码差异。
- 差异审查确认图片桥接条件未变；新增的公共变量只剩 8 月 13 日 Codex OAuth 身份收敛。开始从当前库和 04:35 备份核对目标测试时该功能是否开启。
- 已确认身份收敛在 8 月 13 日开启并覆盖目标请求。开始核对它删除设备证明、重写身份字段后，旧版 Mac 本地图片工具授权是否失效。
- 找到 OpenAI 官方 Codex 仓库中的同症状报告：GPT‑5.6 读取 imagegen skill 后跳过工具调用并虚假报完成；另有 gpt‑5.6-sol 命名空间工具路由问题。开始把目标三次请求按模型和调用顺序与该已知回归对齐。
- 目标时序已与已知回归对齐：两条 5.6 Sol 请求对应“读取 skill → 回传内容 → 虚假完成”，且没有 Images 请求。准备运行现有最小单元测试，确认 Aether 确实在本地 image_gen 存在时关闭托管备用路径。
- 最小测试被工作区现有计费接口编译错误阻断，与本问题无关，未修改。开始核对 wenwen 8 月 3 日成功链使用的模型，验证是否正好是官方报告中可正常生图的 GPT‑5.5。
- 更正：wenwen 上次成功也是 5.6 Sol，同模型、同客户端。当前最强回归点变为 8 月 13 日身份收敛；开始整理它与本地 image_gen 互斥逻辑共同导致失败的证据边界。
- 已补齐 Responses Lite 机制：本地 image_gen 让服务端既不提供托管备用工具，也保留 Sol 的 Lite 路径，正好暴露已知命名空间工具问题。开始形成最终根因结论和无法由现存数据确认的边界。
- 已形成根因结论：不是 wenwen 配置错误；失败机制是 5.6 Sol 本地 image_gen 未调用且 Aether 无托管备用路径，最可能由 8 月 13 日身份收敛与 Sol Lite 工具问题共同触发。现阶段只诊断，未修改生产配置或业务代码。

## 2026-08-14 GPT-5.6 Sol 本地图片工具替换修复

- 用户已授权执行修复。
- 已通过 OpenAI Docs 复核：GPT-5.6 Sol 支持 Responses 托管 `image_generation`，图片结果使用原生 `image_generation_call`。
- 已建立实施计划；当前先更新架构说明并补失败测试，尚未修改业务逻辑或生产配置。
- 已更新图片桥接架构说明，明确只对 GPT-5.6 Sol 替换本地图片工具，并保留其他工具。
- 已新增请求改写测试；测试按预期失败，实际输出仍保留 `image_gen` 且没有注入托管工具，证明测试覆盖了当前缺口。
- 已完成请求改写并通过定向测试：格式层 2 项、网关规范化 1 项、格式模块 24 项、网关规范化模块 18 项、Lite 请求头清理 3 项、原生图片结果改写 1 项全部通过。
- 当前核对 wenwen 所需的原始请求/响应记录链路，重点检查请求头采集、入库前遮盖、完整正文记录和自动清理。
- 已读取生产配置：当前仍为 `request_record_level=basic`，自动清理使用默认开启值。
- 已新增用量明细独立清理开关、架构说明和测试；定向测试确认独立开关、管理端默认值及 Actor Authorization 原值记录均通过。
- 已更新生产诊断配置：`request_record_level=full`，请求/响应正文上限 16 MiB，用量明细相关保留期 3650 天，请求候选保留期保持 30 天；数据库复查值一致。
- 完整回归通过：图片格式模块 24 项、网关规范化 18 项、Lite 清理 3 项、图片原生结果 1 项、维护运行时 54 项、管理配置 17 项、用量运行时 118 项；`cargo fmt --check` 与 `git diff --check` 通过。
- 代码尚未提交或发布；生产当前只启用了完整诊断记录，图片工具替换需发布后由 wenwen 实机验证。

## 2026-08-15 GPT-5.6 图片桥接审查修复

- 已复核四项审查问题及官方协议；开始先更新架构说明，再补失败测试。
- 已更新两份架构说明，明确模型别名、嵌套工具选择、流式本地回退和清理目标边界。
- 新增用例按预期复现：`allowed_tools` 遗留本地图片项、`gpt-5.6` 未替换、流式回退缺少截断状态输入、自动清理无法选择性保留过期 Key。
- 已完成四项实现；图片格式模块 26 项测试全部通过，网关增量编译和定向测试进行中。
- 入库复核发现无效对象引用会覆盖本地回退正文，已限制为仅上传成功的对象暴露顶层引用，失败描述继续保留。
- 最终相关测试共 119 项通过：图片格式 26 项、Codex 请求头 15 项、请求规范化 18 项、维护运行时 54 项、对象存储 4 项、用量流式入库 2 项；格式检查和差异检查通过。
- 本轮未提交、未发布、未执行生产部署。

## 2026-08-15 当前生产版本生图问题复核

- 已从 OpenAI 官方文档重新确认 GPT-5.6 Sol、`gpt-5.6` 别名和托管图片工具协议。
- 已开始核对今天最新主线、生产镜像与当前未提交生图修复的差异；尚未修改业务代码或生产配置。
- 已完成最新主线与当前工作区逐文件对比：上一轮四项审查修复均未进入 `origin/main@f342367ad`，当前必须基于最新主线重新整理发布改动。
- 已确认当前生产部署拓扑和版本核对方式，下一步只读查询两台 Frontdoor 与单活 Background 的镜像 revision 和健康状态。
- 已确认两台生产主机的现有 SSH 入口，未修改服务器状态。
- 已只读核对生产容器：hd0526 已上线最新主线，OVH 仍是旧版且部署状态文件与容器不一致；三项应用容器当前均健康。
- 首次按旧拓扑查询 rn01 失败，核对后确认生产主库已切换到 rn-hybrid；已更换为当前主库继续只读排查。
- 已确认当前用量表保留诊断所需四类正文；首次按冗余用户名查询为空，下一步改用用户表 ID 关联。
- 首次将用户关联和全量计数放在同一条 SQL 中超过 30 秒，已停止该方式；改为先解析用户 ID，再按索引读取最近记录。
- 已按用户 ID 读取 `wenwen` 最近 30 条使用记录：今天部署后没有新请求，旧的生图记录没有保存任何请求体或响应体。
- 首次查询 `system_configs` 时使用了错误的列名，查询失败且未修改数据库；下一步先读取表结构，再按实际列名核对记录与清理配置。
- 两次追加本轮检查结果时，补丁上下文与文件实际内容不一致；均未写入不完整内容，已读取文件尾部后按准确文本更新。
- 已读取当前主库 `system_configs` 的实际结构并按正确列名复查：完整记录级别在数据库切换后变成 `basic`，因此下一次测试仍不会保留请求和响应正文。
- 已确认两台 Frontdoor 均未配置用量对象存储；当前主线缺少本地正文回退，必须和图片工具修复一并发布，才能保证下一次失败可定位。
- 已定位图片桥接配置所在的数据表，下一步按 `wenwen` 最近请求实际命中的 Provider 和 Endpoint 核对开关。
- 已按 `wenwen` 最近请求核对实际 Provider 与 Endpoint：图片桥接开关为开启状态，不需要再改本地 `config.toml` 或该 Provider 开关。
- 已再次核对生产容器，版本与前次结果一致：hd0526 已是本地最新主线，OVH 仍停在旧版且状态文件不一致。
- 两次执行 `git ls-remote` 均在 30 秒内没有返回结果；未修改仓库，改用已获取的 `origin/main` 并通过 GitHub 页面补充确认。
- GitHub 网页工具无法读取私有仓库 API，搜索也没有返回目标仓库；已改用当前登录的 GitHub CLI，确认远端主线最新提交仍为 `f342367ad`。
- 本轮复核完成：未改业务代码、未改生产配置、未发布；已形成基于最新主线的修复与验证顺序。

## 2026-08-16 号池分配模式与策略组合调度修复

- 用户已确认按管理端现有两层语义实施修复，并要求检查全部五种分配模式。
- 已确认根因、缓存亲和绑定边界和超过单页账号的潜在问题；开始先更新行为文档，再补失败测试。
- 已新增两层调度行为文档，并标注旧优先级优先文档的后续组合规则。
- 游标审查发现评分优先读取也会在策略生效前截断账号范围；已纳入分页修复和测试范围。
- 已新增三组失败测试并确认当前代码确实失败：五种分配模式都会遮蔽额度策略；相同额度会被原始顺序提前拆开；70 个账号时第 65 个之后的低用量账号不会进入首轮选择。
- 已完成核心排序实现，核心调度 13 项测试通过；网关实现已加入显式策略的有界全范围比较，等待清理构建缓存后的重新编译验证。
- 超过单页的策略调度用例已通过：70 个账号时能选中第二页的最低用量账号。
- 已完成五种分配模式运行时反馈审查，并让负载均衡、优先级优先停止维护无关 LRU 状态；配置判定和 Redis 反馈测试通过。
- 新增缓存绑定硬性可用检查用例并确认旧代码会错误返回已耗尽账号；已让绑定账号进入统一过滤后再复用。
- 网关号池调度模块现有 37 项测试全部通过，包括缓存绑定硬性过滤、跨页额度选择和 2048 账号懒加载回归。
- 最终验证通过：核心与提供商号池 44 项、网关号池调度 37 项、管理配置与运行时 39 项；`cargo fmt --check` 和 `git diff --check` 均通过。
- 后续评审修复了路由覆盖设置只影响账号读取、不影响最终选择的问题；同一次请求的账号状态读取和最终选择现在使用同一份生效设置。
- 首次回归把服务商自动补充的默认策略误判为用户明确策略，导致 2048 账号 LRU 用例读取 512 个账号；已改为只识别用户明确开启且服务商支持的策略，回归恢复为单页读取。
- 评审修复验证通过：网关号池调度 39 项、核心与提供商号池 44 项全部通过；目标文件格式检查和差异检查通过。
- 已修复成功记录重新读取号池页面设置的问题；本次请求实际使用的路由覆盖设置现已贯穿账号选择、执行计划和成功记录。
- 新增双向覆盖测试并通过：页面缓存亲和、路由负载均衡时不记录缓存绑定/LRU；页面负载均衡、路由缓存亲和时按缓存亲和记录。相关测试共 88 项通过（号池调度 39、成功与失败记录 27、执行信息 17、报告信息 5）。
- 本轮只修改本地代码和架构文档，未提交、未发布、未修改生产配置。

## 2026-08-16 WorkBuddy 调用 Niffler GPT 报错诊断

- 明确本轮角色为后端故障调查，只定位原因，不实施修复。
- 使用 `planning-with-files` 记录跨代码、日志和数据库的证据；保留工作区全部已有改动。
- 已登记用户提供的时间、用户 ID、外层 Request ID、Trace ID 和服务端错误原文，开始定位代码链。
- 已定位精确报错函数和全部比较字段，确认错误发生在同一请求标识的第二次计费准入写入，不是模型上游返回。
- 已发现精确比较包含准入时钱包余额；继续核对近期相关提交与生产数据库实际两份输入的差异来源。
- 已核对相关历史和当前主线，排除 8 月 13 日 numeric 热修引入本次错误；确认重复写入不会覆盖首次准入。
- 已追踪 Planner 到执行写入：正常候选重试会携带同一份准入；开始核对 Trace ID 的来源以及生产中是否出现独立调用复用。
- 已确认网关直接信任入站 `x-trace-id`，本次 32 位无连字符 Trace ID 来自外部，不是 Niffler 自动生成。
- 已验证当前生产数据库主机和两个 Frontdoor 的只读 SSH 入口，准备按精确时间与标识查询。
- 已从 hd0526 日志确认相同 Trace ID 在 7 秒内进入两次：第一次 200，第二次 500；两次鉴权余额不同。
- 已从主库读取首次准入原记录，确认第二次冲突来自重算钱包余额变化；OVH 同期无该请求。
- 已核对用量与候选表约束：用量表对 request_id 唯一，第二次请求失败位置早于用量写入，因此管理端只显示成功记录具有明确的数据结构原因。
- 已读取该 Trace ID 的唯一用量与候选：第一次 `gpt-5.6-sol` 请求成功，计费金额正好解释余额变化；第二次未生成用量或候选记录。
- 已确认 Niffler 用户名为 `kaige`，并排除两台 Caddy 生成 Trace ID。
- 已从首次请求 HTTP 审计确认 WorkBuddy 5.3.13 同时发送 `x-trace-id`、W3C `traceparent` 和独立 `x-request-id`；错误报告显示第二次 Request ID 不同而 Trace ID 相同，根因已定位为 Trace ID 与计费幂等键语义冲突。
- 已检查当天其他同类日志，确认修复应覆盖服务端请求唯一标识和内部准入复用，不能只针对 WorkBuddy 特判。
- 本轮诊断完成：未修改业务代码、生产配置或数据库数据。
- 用户要求再次审查并给出系统解决方案；当前转入架构复审，重点区分追踪标识、服务端请求 ID、客户端幂等键和内部尝试 ID。
- 已复核入口和响应元数据：当前不存在独立服务端请求身份，外部 Trace ID 从中间件一路承担执行、计费、用量和候选主键职责。
- 已枚举身份混用范围，确认 Chat、Responses、图片、视频、文件、转发、内部报告和用量都受影响；下一步审查 Niffler 自生成 UUID 的同类冲突，避免漏掉内部重试缺陷。
- 已完成一例内部 UUID 冲突取证：3 个候选失败后重规划再次写准入并冲突，请求没有产生费用，确认存在独立的“同请求重新计算准入”缺陷。
- 已确认每个候选尝试都会重复执行“解析准入 + upsert”，而用量 pending 建立更晚；开始追踪动态候选源跨页时准入快照如何变化。
- 已排除同一分页游标主动刷新准入 scope；继续追踪候选耗尽后的 fallback 路径。
- 已定位候选耗尽后的 control-execute fallback 会重建计划并再次写准入，且冲突会绕过正常失败用量收尾。
- 已确认重复执行发生在代理层 `maybe_execute_via_control`，不是单次 execution-path 内的 plan fallback；根因链已细化。
- 已确认该重复链由客户端显式 fallback 请求头开启，并发现现有测试缺少候选去重与准入复用覆盖。
- 已根据生产 HTTP 审计纠正归因：内部 UUID 冲突也可能来自 execution path 内多个规划步骤连续运行；后续方案按所有可重入规划链统一处理。
- 已确认 StandardFamily 与 SameFormatProvider 会覆盖相同格式候选，并发现计费金额使用 `f64` 与 PostgreSQL numeric 精确比较的独立数据契约风险。
- 已对照既有架构文档，确认代码违反“请求内复用首次准入”和“所有 AI 响应均有使用记录”两项正式契约；方案将复用现有 usage 生命周期，不新增重复页面。
- 已复核计费金额预留链路，确认预留 UUID 和幂等键也依赖当前 Trace ID，身份修复必须同时覆盖准入、候选、用量、预留和内部隧道。
- 已形成完整方案：入口生成独立服务端 Request ID，请求级上下文唯一持有首次准入与已尝试候选，存储层只校验不可变所有权并返回首次决定，执行步骤去重，使用记录由请求级终态守卫统一收尾。
- 已给出兼容迁移、两节点灰度、监控和跨数据库验收边界；本轮仍未修改业务代码、生产配置或生产数据。
- 用户确认实施。已重新进入开发阶段，保留工作区现有图片、号池和清理改动；本次只修改请求编号、收费决定复用、服务切换去重及失败记录相关文件。
- 已确认 Codex OAuth 指纹收敛与本次修复职责不同：前者维持上游账号、会话和任务身份，后者区分 Niffler 实际收到的每次调用；实现时不得用任何收敛后身份替代内部调用编号。
- 已用新增测试确认旧逻辑存在两项问题：相同跟踪号的独立调用没有各自的内部编号；内部换服务时余额等实时变化会推翻首次收费决定。
- 已开始实现：网关为每次调用生成独立编号，响应返回该编号；使用记录保留客户端跟踪号；同一次调用沿用首次收费决定。
- 已完成内部调用编号拆分：每次 HTTP 模型调用都使用服务端新编号，客户端跟踪号和客户端请求号只用于查询关联；旧执行服务返回的自带编号也会被改回本次内部编号。
- 已完成收费决定复用：同一次调用后续换服务时返回第一次保存的钱包或套餐决定，不再用变化后的余额、套餐范围重新判定冲突；用户、密钥、钱包或模型真的变化时仍会拒绝。
- 已完成重复服务拦截：同一次调用内，相同供应商、地址、账号、上游模型和重试次数只能执行一次；明确增加重试次数或更换地址后才允许继续。
- 已将处理中记录提前到收费决定和服务记录建立之前；该阶段失败会立即写成失败记录，不再等十分钟后由超时清理补写。
- 已让使用记录和服务执行记录保留客户端跟踪号，旧测试也改为按跟踪信息找到新的内部调用编号。
- 定向验证通过：相同跟踪号独立调用 1 项、处理中与正文轻量记录 5 项、数据层收费与服务记录 16 项、重复服务拦截 1 项、钱包结算 1 项、审计查询 8 项、Codex OAuth 流式调用 1 项、价格结算 6 项、服务记录关联 1 项；网关和数据层编译检查、格式检查、差异检查均通过。
- 当前工作区完整本地使用记录模块 16 项中 15 项通过；剩余 1 项是此前流式正文回退已改为本地保存、旧测试仍要求“不可用”，与本次编号和收费修复无关，未覆盖该项用户改动。
- 本轮仅修改本地代码、测试和架构文档，未提交、未发布、未修改生产配置或历史收费数据。

## 2026-08-16 WorkBuddy 请求身份与收费修复代码审查

- 用户要求审查本轮实现；本次只读检查，不修改业务代码。
- 使用 `planning-with-files` 保存跨网关、收费、使用记录和多节点传播的审查证据。
- 已完成入口编号、收费决定、重复服务拦截、失败记录和多节点转发边界审查。
- 发现三个 P1 和一个 P2：后续服务仍重新读取收费条件；部分错误绕过统一失败收尾与内部编号响应；处理中记录不保证在收费和上游调用前写入；普通上游响应可覆盖网关编号。
- 既有正向测试覆盖成功返回内部编号、数据层复用收费决定和重复服务拦截，没有覆盖本次发现的四个跨层失败场景。
- 本次没有修改业务代码、配置或数据，也没有执行提交、发布或生产操作。
- 后续复核“迁移观察”页：确认钱包预占已彻底失效，新调度、错误提示和返利仍会影响真实业务，路由、结算和一致性记录用于迁移对账。
- 确认页面同时存在失效开关和过时提示，建议拆分到正式业务设置与高级运维观察，不应继续以一个“迁移”页承载所有功能。

## 2026-08-16 WorkBuddy 请求身份与收费审查修复

- 用户要求先修复代码审查问题，迁移观察页后续再评估。
- 当前按高风险计费修复处理：先更新行为文档和失败测试，再实现与验证。
- 已先更新行为文档：明确处理中记录必须在首次收费判断前同步写入，所有返回统一带内部调用编号，普通上游不得覆盖该编号，并纠正当前不存在钱包预占的表述。
- 已核对四处实现边界：收费范围仅在单个规划步骤内复用；代理有多处错误绕过统一收尾；处理中写入是后台异步任务；普通上游同名响应头会覆盖内部编号。下一步新增对应失败测试。
- 已完成四项修复：整个调用复用第一次收费条件；“处理中”记录等待数据库确认后再继续；代理入口统一接住内部错误并补内部调用编号；普通服务商的同名编号会被删除。
- 多节点转发继续返回实际执行节点的内部调用编号，避免客户端拿到无法查询的入口编号；只有网关内部转发响应会保留该编号。
- 默认同步生图心跳路径已接入同一份已尝试服务清单，重复的供应商、地址、账号、模型和重试次数不会再次调用。
- 针对性测试已通过：收费条件只读取一次、处理中记录先于执行、普通服务商编号清理、普通错误编号补充、内部网关编号保留、同步生图重复路径拦截。当前进入扩大回归验证。
- 扩大回归完成：网关直接使用记录 6 项、收费条件 1 项、响应编号 4 项、同步生图 8 项、服务执行 5 项、全部服务失败记录 1 项和用量运行时 119 项通过；网关与用量运行时编译、格式检查和差异检查通过。
- 本地使用记录模块共 16 项，在扩大栈空间后 15 项通过；唯一失败是工作区已有的流式正文本地保存改动与旧断言不一致，不属于本次收费和请求编号修复，未擅自改动。
- 已完成“迁移观察”复核：页面当前混放真实业务开关、无效的钱包预占、只读历史数据和返利处理操作；一致性检查还会因已取消的预占产生误报。结论是不按现状长期保留，应先拆分真实功能和历史观察，再移除迁移入口。
- 本轮仅完成本地代码、测试和文档修改，未提交、未发布、未修改生产配置或历史数据。

## 2026-08-16 main 提交与生产发布

- 用户明确要求将当前工作区全部未提交改动提交到 `main`，随后构建并上线。
- 已进入提交前检查；先核对分支、全部变更、敏感文件和项目现有发布脚本，不执行强制推送或生产数据修改。
- 已确认当前分支与远端 `main` 历史分叉，不能直接推送或强制覆盖；将只迁移本次未提交改动到最新 `origin/main`。
- 首次查询 GitHub Actions 时未指定仓库，CLI 错误查询了上游仓库并返回 404；后续固定使用 `ryfineZ/Niffler`。
- 已确认目标仓库两个正式工作流均可用，最近一次 `main` 镜像构建和生产发布均成功；本次继续采用同一条受限发布链路。
- 当前全部改动已保存为提交 `525d8df0a`；敏感内容检查命中的字符串均为测试假 Key。
- 本地 `main` 已快进到最新 `origin/main@f342367ad`；迁移新提交时只有三份过程记录冲突，业务代码全部自动合并。冲突记录已合并保留双方内容。
- 最终 `main` 编译、格式检查、差异检查和前端生产构建通过；调度、收费数据层和用量运行时 241 项测试通过。
- 网关使用记录 48 项中 47 项通过；唯一失败是旧测试仍要求对象存储缺失时丢弃流式正文，与已经记录并实现的本地保存规则相反。已更新该测试，准备重跑完整模块。
- 旧断言修正后，网关使用记录 48 项全部通过；图片与协议格式 389 项、网关号池 39 项、图片同步 8 项、服务执行 5 项和响应编号 4 项也全部通过。
- 最终本地验证合计 734 项通过，前端生产构建、后端编译、格式检查和差异检查通过；当前可以推送 `main` 并启动 CI 生产镜像构建。
